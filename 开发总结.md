# 恒艺美发项目开发总结

这份文件是整个项目的“学习记录”。每完成一个步骤，就在这里追加一段，保证我能看懂项目为什么这样改，而不是只看到最后的代码。

## 当前项目基线

- 后端使用 FastAPI、SQLAlchemy、SQLite/PostgreSQL 配置、规则型 ChatAgent、LangChain 适配器和 RAG 检索。
- 后端已有预约、发型师空档、会员积分、生日营销和留存提醒接口。
- H5 和小程序仍有演示数据，后续需要逐步改成真实 API。
- 当前测试基线为 `8 passed`。
- 已发现测试会操作根目录 `hair_salon.db`，因此第一阶段先解决测试数据库隔离。

## 开发步骤记录

### 第 1 步：数据库与测试基础（已完成）

目标：让自动化测试使用独立数据库，并准备 Alembic 迁移基础，避免测试污染演示数据，也为后续登录、钱包和通知等新表做准备。

#### 做了什么

1. 在 `tests/conftest.py` 中让 pytest 在导入后端前固定使用 `sqlite://`，也就是共享内存数据库。
2. 在 `backend/database/connection.py` 中为内存 SQLite 配置 `StaticPool`，保证不同数据库会话看到同一份测试数据。
3. 每个测试开始前先删表、建表、填充发型师和时间槽示例数据，所以测试之间不会互相污染。
4. 新增 `tests/test_database_isolation.py`，明确检查测试不能连接项目根目录的 `hair_salon.db`。
5. 增加 Alembic 依赖、`alembic.ini`、迁移环境和 `0001_initial_schema` 初始迁移。
6. 在 `AGENTS.md` 增加规则：每完成一步都要在本文件里用小白能看懂的话记录原因、关联、验证和面试讲法。

#### 为什么这么做

以前 pytest 会直接操作根目录的演示数据库，测试可能新增预约、取消预约甚至重建表，导致下一次打开系统时数据变样。现在测试使用内存数据库，程序正式运行时仍然可以通过 `DATABASE_URL` 使用 PostgreSQL 或开发 SQLite。

Alembic 的作用是给数据库结构建立“版本号”。以后增加密码、钱包、通知等表时，可以执行迁移逐步升级数据库，不需要手工删库重建。

#### 和其他模块的关系

数据库连接是所有 API、Agent、会员服务和预约服务的底层依赖。先把测试和迁移基础稳定下来，后续登录、权限、钱包和通知的改动才有可靠的回归验证环境。

#### 验证结果

- `pytest tests/test_database_isolation.py::test_tests_use_a_separate_sqlite_database -q`：通过。
- `pytest -q`：`9 passed`。
- `DATABASE_URL=sqlite:///D:/面试项目/.alembic-verify.db alembic upgrade head`：通过。
- Alembic `downgrade base` 后再次 `upgrade head`：通过。
- 仍有 9 条警告：1 条 TestClient 依赖弃用警告，8 条循环外键删除表警告；它们不影响本步功能，但后续可以单独整理。

#### 面试讲法

> 我先把运行数据库和测试数据库隔离。正式环境可以连接 PostgreSQL，测试使用共享内存 SQLite，并且每个测试前重建数据。数据库结构通过 Alembic 版本化，这样后续新增登录、钱包和通知表时，不需要手工删库，测试也不会污染演示数据。

#### 剩余问题和下一步

- 生产环境的 PostgreSQL 连接参数还需要根据本机账号写入未提交的 `.env`，不能写入模板或代码。
- 现有业务模型还没有密码、钱包、退款、通知和审计字段。
- 下一步进入客户/员工登录和接口鉴权设计；预约归属校验会作为明确的安全验收项。

### 第 2 步：客户/员工登录与预约鉴权（已完成）

#### 做了什么

1. `User` 增加 `password_hash` 和 `is_active`，数据库迁移为 `0002_add_user_auth_fields`。
2. 增加 Argon2 密码哈希和 JWT 工具，密码不会以明文保存。
3. 增加登录接口：
   - `POST /api/auth/register`：客户注册。
   - `POST /api/auth/login`：客户、发型师、管理员统一登录。
   - `GET /api/auth/me`：查看当前登录用户。
4. 增加角色依赖：客户、员工、管理员分别有不同访问范围。
5. 预约创建、查询、取消都从登录 token 获取客户身份，不再相信请求体中的手机号。
6. 客户聊天和 LangChain 聊天接口也要求登录；员工身份会自动使用员工助手角色，不能由客户请求体伪造。
7. 客户列表、生日营销、留存工作台和员工日程增加员工权限；数据库初始化增加管理员权限检查。
8. 环境模板增加 JWT 密钥、员工演示密码和管理员演示账号配置。
9. 增加重复注册、错误密码、未登录访问、客户越权和迁移兼容测试。

#### 为什么这么做

以前接口只要传一个手机号就能访问数据，知道预约 ID 甚至可能取消别人的预约。现在 token 代表真实登录用户，后端从 token 查出用户和角色，再决定能不能执行操作。

密码使用 Argon2 哈希，即使数据库泄露，也不会直接暴露用户密码。JWT 适合 H5 和小程序，因为两个客户端都可以在请求头中携带同一种登录凭证。

#### 和其他模块的关系

登录身份是钱包、退款、通知、审计和前端真实 API 的共同基础。后续所有客户数据都应该通过当前用户 ID 查询，员工工作台则通过角色依赖保护。

#### 验证结果

- `pytest -q`：`15 passed`。
- `pytest tests/test_migrations.py -q`：通过，SQLite 空库可以升级到最新迁移。
- `python -m compileall -q backend migrations`：通过。
- 已验证客户不能取消他人预约，不能访问员工客户列表。

#### 面试讲法

> 我使用 JWT 建立当前用户身份，密码只保存 Argon2 哈希。API 通过统一依赖解析 token 和角色，预约查询和取消都用当前用户 ID 做归属校验，避免只相信前端传来的手机号。客户、发型师和管理员共用登录入口，但通过角色依赖访问不同业务范围。

#### 剩余问题和下一步

- H5 还没有登录页面和 token 请求封装，仍有 `demo-*` 发型师和时间槽数据。
- 小程序还没有真实请求封装，仍主要依赖 `globalData`。
- 员工演示账号需要在本机 `.env` 填写密码后，由初始化脚本创建。
- 下一步把 H5 和小程序的发型师、时间槽、预约和登录接到真实 API。

### 第 3 步：H5 与小程序接入真实预约 API（已完成）

#### 做了什么

1. H5 新增登录/注册页，使用后端 JWT；token 保存在浏览器 `localStorage`，请求自动带 `Authorization`。
2. H5 启动时调用 `/api/auth/me` 和 `/api/members` 恢复当前用户。
3. H5 发型师列表改为调用 `/api/stylists`，排档改为调用 `/api/stylists/{stylist_id}/slots`。
4. H5 预约列表改为调用 `/api/appointments`，预约按钮只调用真实 `POST /api/appointments`，删除了 `demo-*` 分支。
5. 修正 H5 发型师图片路径为后端静态目录 `/static/assets/...`，并通过浏览器验证资源不再 404。
6. 小程序新增登录页和注册/登录流程，token 保存到微信本地存储。
7. 小程序 `app.js` 增加统一 `wx.request` 封装：自动拼接 API 地址、附带 token、处理 401 和错误信息。
8. 小程序预约页、老师排档页、我的预约页和历史页改为读取真实后端数据，不再依赖本地发型师和时间槽数组。
9. 正式演示 SQLite 旧库先执行 `alembic stamp 0001_initial_schema`，再升级到 `0002`，没有删除原有数据。

#### 为什么这么做

之前两个客户端各自维护一套假数据，前端显示的老师和时间可能根本不存在于数据库。现在客户端只负责展示，发型师、排档、预约状态都来自同一套后端接口，客户预约后刷新页面仍能看到真实记录。

#### 和其他模块的关系

客户端真实 API 依赖第 2 步的 JWT 身份；预约接口又依赖后端的归属校验。后续钱包、通知和员工管理端也会复用同一个请求封装和登录 token。

#### 验证结果

- Playwright 浏览器流程：未登录显示登录页；注册并登录后显示当前用户；预约页加载真实张三等发型师；排档页加载数据库时间槽；点击时间槽后“我的预约”出现真实预约。
- H5 控制台资源 404 已修复。
- `pytest -q`：`15 passed`。
- 小程序全部 JS 通过 `node --check`。
- 小程序全部 JSON 使用 Node `JSON.parse` 验证通过。
- `rg "demo-" frontend`：无演示 ID。

#### 面试讲法

> H5 和小程序没有各自维护业务数据，而是共享后端 API。登录后客户端保存 JWT，请求自动带身份；发型师、时间槽和预约都从数据库读取，所以预约结果在不同客户端刷新后仍然一致。前端只负责页面状态，后端负责业务事实和权限校验。

#### 剩余问题和下一步

- 小程序的 `apiBase` 需要按微信开发者工具或真机网络环境改成后端局域网地址。
- 充值、退款、积分流水和通知页面还在使用演示状态，下一步需要先补钱包流水、退款申请和站内通知模型及 API。
- H5 的充值/退款按钮目前仍是前端本地加减，尚未接入真实资金流水。

### 第 4 步：业务模型补齐（已完成，等待审核）

#### 本阶段目标

把“账户有多少钱、积分为什么变化、退款由谁处理、通知是否已读、关键操作是谁做的”这些业务事实，变成数据库里可查询、可追溯的记录；同时先建立 Agent 任务状态表，为后面的人工作确认流程打基础。

#### 做了什么

1. 新增钱包账户和钱包流水。余额统一用“分”保存为整数，例如 200 元保存为 `20000`，避免小数计算出现精度误差。充值或退款不会只改余额，都会留下流水。
2. 新增退款申请。客户发起退款时余额不立即变化；员工或管理员审核通过后，才扣余额、写退款流水、写审计记录并发送站内通知。客户不能审核自己的退款。
3. 新增积分流水。每次积分变化都记录“变了多少、变化后剩多少、为什么变化”，客户可查询自己的积分明细。
4. 新增站内通知和已读状态。客户只能读取、标记自己的通知，不能操作别人的通知。
5. 新增审计日志。充值、退款审核、积分变动等关键动作会记录操作者、动作、业务对象和时间，方便排错和面试演示。
6. 新增 Agent 任务状态表。它保存发起人、任务类型、输入/输出、执行状态和“是否等待人工确认”。**当前它只是数据底座，没有接入 LangGraph，也没有调用大模型。**
7. 新增 Alembic 迁移 `0003_add_finance_and_notification_tables` 和 `0004_add_agent_task_states`。现有演示数据库已安全升级到最新版，没有删除预约、用户或钱包演示数据。
8. H5 和小程序的充值、退款、积分明细、站内通知页面改为真实后端接口，不再由前端本地伪造余额或通知。

#### 关键流程

```text
客户申请退款
  -> 后端创建“待审核”退款单，余额不动
  -> 员工/管理员审核通过
  -> 一次数据库提交：扣余额 + 写退款流水 + 写审计日志 + 发站内通知
  -> 客户刷新后看到新余额和通知
```

退款申请和扣钱分成两步：员工未同意前，客户的钱不会被提前扣掉；同意后，相关记录要么一起成功，要么一起失败，避免出现“钱扣了但没有记录”的半完成状态。

#### 和其他模块的关系

- 登录与权限：钱包、退款、通知都从 JWT 识别当前用户，不相信前端自己传来的用户 ID。
- H5 和小程序：两端复用真实 API，所以余额、积分和通知刷新后保持一致。
- 数据库迁移：通过 Alembic 新增表，不需要删库重建。
- 后续 Agent：任务状态表先保存 Agent 过程和人工确认状态；以后模型只能提建议，不能绕过确认直接改关键业务数据。

#### 验证结果

- `alembic upgrade head`：演示数据库成功升级到 `0004_add_agent_task_states`。
- `pytest tests/test_migrations.py -q`：`1 passed`，空 SQLite 数据库可从零升级到最新结构。
- `pytest tests/test_agent_task_state.py -q`：`1 passed`，Agent 状态可保存发起人、默认待处理状态和人工确认标记。
- `pytest -q`：`20 passed`，覆盖登录鉴权、预约归属、迁移、钱包充值、退款审核、通知已读和 Agent 状态模型。
- `python -m compileall -q backend migrations`、小程序 14 个 JavaScript 文件和 17 个 JSON 文件检查均通过。
- `git diff --check` 未发现空白符错误；仅有 Windows 换行符转换提示，不是代码错误。

仍有两类已知警告：第三方 TestClient 的弃用警告，以及 SQLite 删除存在循环外键的测试表时的警告。它们没有造成测试失败，也没有影响第 4 步功能；后续可以单独处理，不能说成已解决。

#### 面试讲法

> 钱包余额不是唯一事实，我为每次资金变化保留流水，并用整数分避免金额精度问题。退款采用“申请-审核-执行”三段式：申请不扣钱，审核通过后余额、流水、审计和通知在一次事务里完成。这样能防止越权，也能在问题发生时追溯是谁、何时、做了什么。AI 部分先保留任务状态和人工确认边界，模型未来只能辅助建议，关键写操作仍由人确认和后端事务完成。

#### 审核边界与下一步

第 4 步到这里结束，当前等待你的审核。没有开始第 5 步的客户业务 API 扩展、员工管理 Web、LangGraph、留存 Agent 或部署工作。

### 第 5 步：客户业务 API（已完成，等待审核）

#### 本阶段目标

让客户侧需要的业务数据都有明确的后端接口：预约、发型师和时间槽、会员资料、个人资料、充值、退款申请、积分流水和站内通知。后端从登录 token 识别客户，前端不再传一个手机号就代表身份。

#### 做了什么

1. 预约接口支持查询、创建和取消；取消时后端同时检查预约 ID 和当前用户 ID，客户不能取消别人的预约。
2. 会员接口支持当前客户创建/更新会员资料，以及按权限读取自己的会员信息；员工读取客户列表仍受员工角色保护。
3. 新增 `PATCH /api/profile`，客户可以保存姓名和 `MM-DD` 格式生日，手机号作为登录账号只读，避免页面修改了手机号但账号身份没有同步。
4. 钱包、退款、积分、通知接口沿用第 4 步的数据模型和事务规则，客户只能访问自己的数据，员工才可以审批退款。
5. 请求参数由 Pydantic 校验，金额必须大于 0，生日格式不正确会返回 422，未登录或越权会返回明确的 401/403/404。

#### 面试讲法

> 客户接口的核心不是“有几个 URL”，而是每个请求都从 JWT 还原当前用户，再用用户 ID 做查询条件。手机号、预约 ID 和会员 ID 都只是业务字段，不能用来替代身份校验。这样前端即使传错参数，也不能读写别人的数据。

### 第 6 步：H5 和小程序接真实接口（已完成，等待审核）

#### 本阶段目标

两个客户端都只负责展示页面状态，发型师、时间槽、预约、会员、钱包、退款、积分和通知全部来自同一套后端 API。

#### 做了什么

1. H5 继续使用统一 `apiRequest`，自动带 JWT，处理 401 登录失效和接口错误；小程序继续使用 `app.request`，统一带 token 并处理错误提示。
2. 两端的个人中心会从 `/api/auth/me`、`/api/members`、`/api/wallet`、`/api/points/transactions` 和 `/api/notifications` 刷新真实数据。
3. 两端个人资料页都能保存姓名和生日；手机号改为只读，避免产生“改了页面、没改账号”的假象。
4. 两端预约列表都有取消入口，调用真实 `DELETE /api/appointments/{id}`，取消后重新拉取列表；已取消或已完成预约不会重复显示取消按钮。
5. H5 和小程序到店记录页移除了写死的示例消费记录，改为展示后端返回的已完成预约；没有记录时显示空状态。

#### 验证结果

- `pytest -q`：`22 passed`；包含个人资料保存、生日格式校验、登录、预约归属、钱包充值、退款审批、通知已读和 Agent 状态。
- 浏览器实际流程：注册/登录 -> 个人资料保存成功 -> 读取真实发型师和时间槽 -> 创建预约 -> 点击取消后显示“已取消”。
- H5 内嵌 JavaScript 语法检查通过；小程序 14 个 JavaScript 文件和 17 个 JSON 文件检查通过。
- `rg` 检查前端已无 `demo-*` ID 和写死的历史消费记录。

#### 审核边界与下一步

第 5、6 步完成时的阶段记录：当时第 7 步员工管理 Web、第 8/9 步 LangGraph Agent 和后续运营留存尚未开始；后续阶段已在本文件后面补齐。

#### 第 6 步补充修正：预约页重复演示卡片

检查浏览器页面后确认，后端示例库返回了 8 条不同的发型师记录，客户预约页只需要展示前 4 条主要演示卡片。H5 和小程序现在都限制客户侧最多显示 4 条，隐藏截图中多出来的 4 张卡片；后台数据库和员工数据没有删除，避免把可能有用的员工记录做破坏性删除。这个限制只属于客户演示页面，未来接入正式员工管理后可以改成分页或按门店筛选。

### 第 7 步：员工管理 Web（已完成）

#### 本阶段目标

提供一个和客户 H5、小程序分开的员工运营工作台。员工登录后可以查看当天预约、客户和会员、退款申请、留存提醒、审计记录，并从同一个页面进入员工查询助手。

#### 做了什么

1. 新增 `frontend/staff.html`、`frontend/staff.css` 和 `frontend/staff.js`，独立实现员工端登录、侧边导航、工作概览、预约、客户会员、退款、留存、审计和员工助手页面。
2. 员工端登录仍然调用 `POST /api/auth/login`，token 只保存在浏览器的员工端存储键中；进入工作台后再次调用 `/api/auth/me`，确认当前账号角色是发型师或管理员。
3. 工作概览同时读取 `/api/staff/schedule`、`/api/customers`、`/api/members`、`/api/refunds` 和 `/api/retention/reminders`，展示今日预约、会员数量、待审核退款和待跟进客户数量。
4. 退款通过或拒绝、留存提醒标记为已联系或忽略，都调用已有后端接口。前端不直接改数据库，后端仍然负责权限、事务、审计和通知。
5. 员工助手调用已有 `/api/chat/langchain` 入口，并在页面明确标识当前为只读查询；本阶段没有把 Agent 修改能力混入员工页面。
6. `backend/main.py` 新增 `/staff` 页面入口，`tests/test_staff_web.py` 验证员工页面可以从 FastAPI 正常返回。

#### 为什么这么做

客户端和员工端的使用目标不同。客户端关心自己的预约、余额和通知；员工端关心全店的预约、客户运营和待处理任务。如果把两者混在同一个页面，权限边界和演示流程都会变得不清楚，所以员工端使用独立入口和独立页面。

员工页面只负责组织数据和触发操作，真正的业务规则仍然在后端。例如员工点击“通过退款”时，后端才会在事务中扣余额、写流水、写审计和发通知。这样前端不能绕过后端直接伪造业务结果。

#### 和其他模块的关系

- 第 2 步提供员工 JWT 登录和 `require_staff` 权限依赖。
- 第 4 步提供钱包、退款、通知、审计和 Agent 状态数据模型。
- 第 5 步提供客户、会员、退款、留存和员工日程接口。
- 第 8 步已在员工页面的“员工助手”入口上完成 LangGraph 只读查询流程。

#### 验证结果

- `pytest -q`：`23 passed`。
- `node --check frontend/staff.js`：通过。
- `git diff --check`：未发现新增代码的空白符错误。
- `GET /staff` 的 FastAPI TestClient 测试通过，并确认页面包含“员工运营工作台”。
- 内置浏览器对本地 `localhost` 和 `file://` 页面被安全策略拦截，因此本阶段没有把浏览器截图作为验证结果；后续可在本机浏览器打开 `http://localhost:8000/staff` 做视觉验收。

#### 面试讲法

> 我把客户侧和员工侧拆成两个入口。员工端通过 JWT 登录后，调用后端已有的员工日程、客户、会员、退款、留存和审计接口，聚合成一个运营工作台。前端只负责展示和发起操作，退款审批等关键规则仍由后端执行，并在事务中留下流水、审计和通知。这样既方便员工处理日常工作，也保持了清晰的权限和数据一致性边界。

#### 验收边界和下一步

第 7 步的员工管理 Web 已完成。员工页面中的查询、运营分析和预约调整入口会分别调用第 8～11 步的 Graph 和通知接口。

### 第 8 步：员工 LangGraph 查询与 RAG（已完成）

#### 本阶段目标

让员工助手通过一个可追踪的 LangGraph 读取真实业务数据，并把实时数据库查询和护理知识 RAG 查询区分开。

#### 做了什么

1. 新增 `backend/agents/staff_graph.py`，使用 `StateGraph` 编排“意图识别 -> 条件路由 -> 查询工具 -> 回答整理”。
2. 增加预约日程、客户历史、会员余额积分、留存提醒和护理知识五类受控查询；所有工具都是只读工具。
3. 新增 `POST /api/staff/agent/query`，员工身份由 JWT 确定，返回 `actions`、`sources` 和持久化的 `AgentTaskState`。
4. RAG 复用已有检索器：优先 Chroma，未配置 embedding 或 Chroma 失败时回退本地 BM25；实时预约和余额不进入知识库。
5. 员工工作台助手改为调用新接口，不再复用客户侧 LangChain fallback。

#### 面试讲法

> LangChain 负责模型和工具适配，LangGraph 负责员工查询流程编排。员工问题先分类，再进入对应的只读工具，最后把工具名和数据来源返回给页面。这样模型不能直接访问数据库，答案也能追溯是来自 SQLAlchemy 查询还是 RAG 文档。

#### 验证结果

- `pytest -q tests/test_staff_graph.py`：`7 passed`。
- 断言员工查询、RAG 来源和客户越权均通过。
- 断言 `build_staff_query_graph()` 实际返回已编译 Graph，不是 fallback。

### 第 9 步：预约调整与人工确认（已完成）

#### 本阶段目标

让 Agent 可以读取预约并生成调整方案，但只有员工明确确认后才允许修改预约。

#### 做了什么

1. 新增 `backend/agents/appointment_change_graph.py`，包含读取校验、人工确认门和执行分支。
2. 新增 `POST /api/staff/agent/appointment-change/propose`，只保存方案和等待确认状态，不改预约和时间槽。
3. 新增 `POST /api/staff/agent/tasks/{task_id}/confirm`，确认后重新校验时间槽占用状态。
4. 新增事务执行器：释放旧时间槽、占用新时间槽、更新预约、写审计、发客户通知在同一个事务中完成。
5. 员工工作台增加预约调整表单和确认/拒绝按钮。

#### 面试讲法

> AI 生成的是 proposal，不是最终业务结果。提议阶段不写库，员工确认后才进入事务节点；确认阶段还会重新检查时间槽，避免等待期间发生并发占用。成功后预约、审计和客户通知一起提交，失败则一起回滚。

#### 验证结果

- `pytest -q tests/test_appointment_change.py`：`5 passed`。
- 已验证确认前预约时间槽不变化。
- 已验证确认后预约变更、客户通知和审计结果都存在。
- 已验证重复确认会被拒绝。

### 第 10 步：客户站内通知（已完成）

#### 做了什么

1. 复用第 4 步已有通知表和客户通知接口。
2. 预约调整确认写入 `appointment` 类型通知。
3. 退款审批继续写入 `refund` 类型通知。
4. 留存提醒标记为已联系时写入 `marketing` 类型通知，并记录员工审计。
5. 客户 H5 和小程序继续通过刷新读取通知，保留已读/未读状态，不引入 WebSocket。

#### 面试讲法

> v1 采用站内通知加刷新，满足业务闭环且部署简单。通知由后端业务动作产生，前端只是读取；因此预约变更、退款和运营触达在不同客户端刷新后都能看到一致结果。

#### 验证结果

- `pytest -q tests/test_wallet.py`：通知和已读权限通过。
- `pytest -q tests/test_appointment_change.py`：预约变更通知通过。
- `pytest -q tests/test_retention_agent.py`：留存触达通知通过。

### 第 11 步：留存运营 LangGraph（已完成）

#### 做了什么

1. 会员新增 `expires_at` 字段，迁移为 `0005_add_member_expiry`。
2. 新增 `backend/agents/retention_graph.py`，按“流失风险、余额客户、会员到期”三个维度分层。
3. 新增 `POST /api/retention/agent/run`，返回可解释建议并保存执行结果到 `AgentTaskState`。
4. 员工工作台增加运行运营分析入口，展示分层数量和建议话术。
5. Graph 只生成建议，不自动发送；员工实际触达仍然是显式操作。

#### 面试讲法

> 留存 Agent 不是凭感觉给客户打标签，而是根据最近到店时间、钱包余额和会员到期时间生成可解释分层。结果写入任务状态，员工决定是否联系，避免 AI 自动骚扰客户。

#### 验证结果

- `pytest -q tests/test_retention_agent.py`：`4 passed`。
- `pytest -q tests/test_migrations.py`：空 SQLite 数据库可升级到 `0005_add_member_expiry`。
- 断言实际编译了留存 LangGraph，并能返回余额客户和会员到期客户。

### 第 12 步：全链路测试和部署准备（已完成）

#### 做了什么

1. 新增员工 Graph、预约确认、留存 Graph、通知和员工页面测试。
2. `requirements.txt` 增加 `langgraph>=1.0.0,<2.0.0`。
3. README 补充员工入口、Agent 接口、RAG 降级策略和启动检查。
4. 新增 `docs/运行与部署.md`，说明 SQLite 测试、PostgreSQL 正式环境、Alembic 和模型配置。

#### 验证结果

- `pytest -q`：`42 passed`。
- `python -m compileall -q backend migrations`：通过。
- `node --check frontend/staff.js`：通过。
- Alembic 空库迁移到最新版本：通过。
- 当前剩余的是第三方 TestClient 弃用警告和 SQLite 循环外键删除表警告，不是测试失败；正式环境 PostgreSQL 需要在部署环境执行实际连通性检查。

### 第 13 步：面试材料整理（已完成）

新增材料：

- `docs/架构与流程.md`：总体架构、只读查询、人工确认和 RAG 流程图。
- `docs/面试演示脚本.md`：3 分钟现场演示顺序。
- `docs/技术选型与面试问答.md`：FastAPI、SQLAlchemy、PostgreSQL、LangChain、LangGraph、RAG、MCP 和事务边界的复述方式。
- `docs/运行与部署.md`：启动、迁移、模型和检查命令。

#### 最终面试主线

> 这是一个有真实业务数据和权限边界的美发门店运营系统。客户侧负责预约和查看，员工侧负责运营处理；LangChain 提供模型和工具适配，LangGraph 编排只读查询、人工确认和留存分析；RAG 负责护理知识，SQLAlchemy 负责实时业务数据；关键写操作由后端事务、审计和站内通知共同保证。

### 补充：本地演示管理员账号（已完成）

本地演示管理员账号由 `.env` 配置，不写入前端或代码。当前账号已同步到本地数据库，并验证可以登录员工工作台。初始化脚本改为按手机号查找并更新示例员工，重复运行不会重复创建员工。

#### 验证结果

- 登录接口返回成功，角色为 `admin`。
- `GET /staff` 返回 `200`。
- `pytest -q tests/test_init_idempotency.py`：通过。
- 全量测试：`42 passed`。

密码只保存在本机 `.env`，不在开发总结、README 或聊天回复中记录。

### 第 8 步补验记录：员工 LangGraph 查询与 RAG（本轮完成）

#### 本轮补齐的内容

1. 保留原有“意图识别 -> 只读查询 -> 回答整理”的 LangGraph 结构，补强员工自然语言日期识别，支持“明天的预约”“今年 7 月 12 日预约”等表达。
2. 修复日期词被姓名正则误识别的问题。例如“明天的预约有哪些”不能把“明天”当成客户姓名，必须路由到全店日程查询。
3. 增加会员余额/积分和留存提醒的真实数据库查询测试，确认不是写死回复。
4. 增加 AgentTaskState 回读测试，确认员工查询结果会保存任务状态、查询动作和来源。
5. 增加失败隔离测试，确认 Graph 查询失败时任务会标记为 `failed`，普通的发型师查询接口仍然可以使用。

#### 和其他模块的关系

- 员工 JWT 和 `require_staff` 决定谁能进入 Graph；客户账号仍然不能访问员工查询接口。
- 预约、会员、钱包和留存提醒属于实时业务数据，Graph 直接调用 SQLAlchemy 查询；护理问题继续通过 RAG 检索，二者没有混用。
- `AgentTaskState` 只记录员工查询过程，不参与预约修改；预约修改仍然属于第 9 步的人工确认和事务流程。

#### 验证结果

- `pytest -q tests/test_staff_graph.py`：`7 passed`。
- `pytest -q`：`42 passed`，保留 1 条第三方 TestClient 弃用警告，没有测试失败。
- `git diff --check`：没有发现空白符错误。

#### 面试讲法

> 第八步的员工助手是一个只读 LangGraph。它先识别员工想查日程、客户、会员、留存还是护理知识，再调用受控查询节点；实时业务数据查数据库，护理知识查 RAG。接口返回 `actions` 和 `sources`，并把任务状态保存到 `AgentTaskState`，所以回答能追溯、失败能记录，而且不会因为 Agent 查询失败影响普通预约接口。真正修改预约的能力不放在这个 Graph 里，而是留到人工确认流程。

#### 下一步

第八步的只读查询验收已补齐。下一步继续检查第九步预约调整流程的人工确认、并发占用校验、事务、审计和客户通知边界。

### 第 9 步补验记录：预约调整与人工确认（本轮完成）

#### 本轮补齐的内容

1. 在确认事务中对预约和目标时间槽使用 `with_for_update()`，PostgreSQL 下确认时会锁住关键行，减少多个员工同时处理同一时间槽的竞争。
2. 增加拒绝方案测试，确认拒绝只结束 Agent 任务，不修改预约、时间槽或客户通知。
3. 增加重复确认测试，确认已经处理的任务不能再次执行。
4. 增加时间槽冲突测试，模拟员工等待确认期间目标槽被别人占用，确认接口返回冲突，原预约保持不变。
5. 补强成功路径断言，确认旧时间槽释放、新时间槽占用、审计记录和客户通知同时存在。

#### 和其他模块的关系

- 员工 JWT 和 `require_staff` 仍然负责确认接口权限，任务只能由创建它的员工确认。
- 预约、时间槽、审计和通知由同一个 SQLAlchemy Session 执行；业务写入成功后才提交，异常由接口回滚。
- 第八步只读 Graph 只负责查询和建议，第九步才允许写入预约，而且必须经过员工明确确认。

#### 验证结果

- `pytest -q tests/test_appointment_change.py tests/test_end_to_end_agent_flow.py`：`6 passed`。
- `pytest -q`：`42 passed`，只有第三方 TestClient 弃用警告，没有测试失败。
- `git diff --check`：没有发现空白符错误。

#### 面试讲法

> 预约调整采用提议和确认两阶段。提议阶段只读取数据并保存 proposal，员工确认时重新读取并锁定预约和目标时间槽；如果时间槽已经被占用，就返回冲突且不改变原预约。确认成功后，释放旧槽、占用新槽、修改预约、写审计和发通知在同一个事务里提交，避免出现只改了预约但没有通知客户的半完成状态。

#### 下一步

第九步的人工确认边界和并发冲突验收已补齐。下一步继续检查第十步站内通知在 H5、小程序和权限接口中的读取、已读状态和业务触发覆盖。

### 第 10 步补验记录：客户站内通知（本轮完成）

#### 本轮补齐的内容

1. 补充退款审批后的通知断言，确认退款业务完成后客户能收到 `refund` 类型通知。
2. 补充通知归属隔离测试，确认另一个客户拿到通知 ID 也不能把通知标记为已读。
3. 保持 H5 和小程序通过 `/api/notifications` 刷新读取，不引入 WebSocket 或额外消息中间件。

#### 和其他模块的关系

- 预约调整、退款和留存触达是通知的三个业务触发点，通知统一写入通知表。
- JWT 确定当前客户，后端查询和已读操作都按 `user_id` 做归属过滤；前端不直接修改通知状态。
- 通知写入和对应业务动作在同一事务中提交，客户刷新后看到的是后端已经确认的结果。

#### 验证结果

- `pytest -q tests/test_wallet.py`：`5 passed`。
- `pytest -q tests/test_appointment_change.py tests/test_retention_agent.py`：相关通知场景通过。
- `pytest -q`：`42 passed`，只有第三方 TestClient 弃用警告，没有测试失败。

#### 面试讲法

> v1 的通知采用站内通知加刷新。后端业务动作负责创建通知，客户端只读取自己的通知；预约变更、退款和留存触达都复用同一张通知表。这样实现简单、权限边界清楚，也不需要引入 WebSocket，就能完成客户刷新后看到业务结果的闭环。

### 第 11 步补验记录：留存运营 LangGraph（本轮完成）

#### 本轮补齐的内容

1. 验证留存 Graph 的分层结果会保存到 `AgentTaskState`，任务状态为 `completed`，结果内容和接口返回一致。
2. 验证运行留存分析只生成建议，不会自动创建客户通知；员工明确点击“已联系”后才写营销通知和审计。
3. 验证客户不能访问员工留存 Agent，分析失败时任务标记为 `failed`，普通发型师查询接口仍然可用。

#### 和其他模块的关系

- 钱包余额、会员到期时间和最近到店时间来自实时数据库，不放入护理知识 RAG。
- `AgentTaskState` 记录分析请求和结果，方便员工复盘，但不代表已经联系客户。
- 留存触达仍由员工工作台显式执行，通知和审计沿用第十步的统一机制。

#### 验证结果

- `pytest -q tests/test_retention_agent.py`：`4 passed`。
- `pytest -q`：`42 passed`，只有第三方 TestClient 弃用警告，没有测试失败。
- 已验证留存 Agent 实际使用编译后的 LangGraph，不是 fallback 顺序执行。

#### 面试讲法

> 留存 Agent 负责发现客户，不负责替员工发消息。它根据最近到店、钱包余额和会员到期时间生成可解释分层，结果保存到任务状态；员工确认联系后，系统才写客户通知和审计。这样可以提高运营效率，同时避免 AI 自动骚扰客户。

#### 下一步

第十步和第十一步的通知、权限、任务持久化和人工触达边界已补齐。下一步进行最终交付审计：同步所有文档中的测试数量，检查启动命令、迁移流程和前端脚本验证结果。

### 最终交付审计：2026-07-11

- `pytest -q`：`42 passed`，只有第三方 TestClient 弃用警告。
- `pytest -q tests/test_migrations.py tests/test_init_idempotency.py`：`2 passed`。
- `python -m compileall -q backend migrations`：通过。
- `python -m compileall -q tests`：通过。
- `node --check frontend/staff.js`：通过。
- 小程序目录下全部 JavaScript 文件执行 `node --check`：通过。
- `git diff --check`：没有发现新增代码的空白符错误；输出中的 CRLF 提示是 Git 换行格式提示，不是测试失败。

当前剩余的环境性事项是正式 PostgreSQL 实例连通性需要在部署环境执行；本地 SQLite 隔离、Alembic 迁移和业务测试已经通过。项目可以按 README 和 `docs/运行与部署.md` 的命令启动和演示。

### 第 14 步：最终运行验收与交付记录（2026-07-12）

#### 本步完成了什么

1. 重新执行全量测试和静态检查，确认当前工作树仍然可运行。
2. 通过 `python -m backend.database.init_db` 幂等初始化本地演示库，确认示例员工、时间槽和演示管理员可用。
3. 启动 FastAPI 服务 `http://127.0.0.1:8000`，实际访问健康接口、客户首页、员工首页和 Swagger 文档。
4. 用管理员账号走了一次真实 HTTP 流程：登录拿 JWT，调用员工只读 Agent，回读 `AgentTaskState`；随后分别验证数据库日程查询和 RAG 护理知识查询。
5. 检查 PostgreSQL 部署条件：本机 5432 端口可达，`psycopg2` 已安装，但 `.env` 的 `DATABASE_URL` 为空，使用无密码连接探测时被 PostgreSQL 拒绝认证。因此本机演示继续使用 SQLite；正式 PostgreSQL 登录必须在部署环境补充真实连接串和密码。

#### 员工只读 Agent 的完整流程

```text
员工输入问题
  -> require_staff 校验 JWT 角色
  -> 创建 AgentTaskState，状态 running
  -> staff_graph.classify_request 判断意图
  -> 预约/客户/会员/留存走只读数据库工具
  -> 护理知识走 RAG（Chroma 不可用时回退 BM25）
  -> format_response 整理 reply、actions、sources
  -> 保存任务 completed 和 result_payload
  -> 返回前端
```

对应代码边界如下：

- `backend/api/routers.py`：负责鉴权、创建任务、调用 Graph、保存成功或失败状态、返回 API 响应。
- `backend/agents/staff_graph.py`：负责意图识别、条件路由、只读工具和回答整理。
- `backend/agents/tools.py`：集中放受控工具，工具不提供预约、钱包或通知写入能力。
- `backend/rag/retriever.py`：负责护理知识检索和 RAG 降级。
- `backend/database/service.py`、`backend/staff/schedule.py`：读取实时业务数据。
- `backend/database/models.py`：定义 `AgentTaskState`，保存工作流类型、输入、结果和状态。

#### 本次真实 HTTP 验证证据

- `/api/health`、`/`、`/staff`、`/docs`：均返回 `200`。
- 管理员登录：成功返回 JWT。
- 员工问题“今天有哪些预约”：任务 `completed`，动作链为 `intent:schedule -> tool:get_salon_schedule`，来源为 `database:staff_schedule`。
- 员工问题“染发后如何护理”：任务 `completed`，动作链为 `intent:knowledge -> tool:search_knowledge`，返回 3 个 RAG 来源。
- 通过任务 ID 回读：`workflow_type=staff_readonly_query`、`status=completed`、`awaiting_confirmation=false`。

#### 最终验证结果

- `pytest -q`：`42 passed`，只有 1 条第三方 TestClient 弃用警告。
- `python -m compileall -q backend migrations tests`：通过。
- `node --check frontend/staff.js` 和小程序全部 JavaScript 文件检查：通过。
- `git diff --check`：没有新增代码空白符错误。
- `Test-NetConnection 127.0.0.1 -Port 5432`：端口可达；PostgreSQL 账号认证因未提供密码未通过，这是部署凭据缺失，不是 SQLite 演示失败。

#### 最后一起复述的面试话术

> 员工只读 Agent 的关键设计是把“理解问题”和“执行写操作”分开。JWT 先保证只有员工能进入接口，任务表记录这次请求；LangGraph 只在几个受控节点之间路由，实时预约和会员数据查数据库，护理问题查 RAG。工具没有写入能力，所以即使问题识别错，也不会直接改预约。最后把动作、数据来源和结果一起保存并返回，既方便前端展示，也方便排错和审计。预约修改则是另一个必须人工确认的 Graph，确认后才进入事务执行。

#### 交付状态

Agent 实现、测试、运行演示、部署文档和面试材料均已完成。当前代码默认可用 SQLite 离线演示；正式 PostgreSQL 的最后一个外部前置条件是部署环境提供有效连接串和密码，之后按 `docs/运行与部署.md` 执行 `alembic upgrade head` 即可。

### 第 15 步：员工工作台交互与登录失效修复（2026-07-12）

#### 本步改了什么

1. 工作概览的“今日预约、会员客户、待处理退款、待跟进客户”四个统计卡片现在可以点击，分别切换到对应的员工模块。
2. 卡片使用真正的 `button` 语义，支持鼠标点击、键盘 Tab 聚焦和 Enter 操作，并增加悬停、聚焦反馈，让用户知道它不是一块静态数字。
3. 员工助手遇到 JWT 过期或无效时，统一清理本地旧 Token 和当前员工信息，直接回到登录页并提示“登录已失效，请重新登录”。
4. 修复了原来的显示错误：`api()` 已切换到登录页后，助手的异常处理又重新渲染工作台，导致页面停留在工作台并显示“查询失败：登录已失效”。现在认证失效错误会停止后续工作台渲染，不会覆盖登录页。
5. 给员工端脚本增加版本查询参数，避免浏览器继续使用旧缓存，确保修复后的交互能被实际加载。

#### 和其他模块的关系

- 卡片导航只改变 `state.view`，复用原有员工工作台的单页视图切换，不新增后端接口。
- 登录失效仍由后端 `401` 响应触发；前端只负责清理 Token、展示登录页，重新登录后再通过原有接口获取新 JWT。
- 预约、客户、退款和留存页面的数据读取逻辑没有改变，只有入口变得更直接。

#### 面试讲法

> 工作台是一个轻量单页应用，侧边栏和概览卡片都通过 `state.view` 切换同一套视图，所以不需要为每个模块重新加载整页。认证失败时，后端返回 401，前端清掉旧 JWT 并回到登录页；不能继续拿失效身份请求业务接口，也不能让异常渲染覆盖登录页。

#### 验证方式

- `node --check frontend/staff.js`：通过。
- 重新登录后点击四个概览卡片，预期分别进入 `今日预约`、`客户与会员`、`退款处理`、`留存提醒`。
- 在浏览器开发者工具中删除 `hengyi_staff_access_token` 或等待 Token 过期，再提交员工助手问题，预期回到员工登录页，不再显示“查询失败：登录已失效”。

### 第 16 步：员工助手界面清理（2026-07-12）

#### 本步改了什么？

- 员工助手回答下方不再显示 `调用`、`来源` 等内部技术链路。
- 删除页面中的 LangGraph 开发说明，只保留员工实际使用的查询界面。

#### 为什么这样改？

这些信息对开发排查有帮助，但对员工没有直接业务价值，会让回答看起来像调试日志。后端仍然保留任务结果和来源信息，前端只是不把内部实现细节展示给员工。

#### 面试怎么讲？

> 我把后端的可追踪信息和前端的用户展示分开：后端保留调用链和数据来源用于审计、排错，员工端只展示最终答案，避免把 LangGraph、工具名等实现细节暴露给业务用户。

#### 下一步

重新加载员工助手，提交一条查询，确认页面只显示问题和最终回答，不显示内部调用链路。

### 第 17 步：留存提醒显示常用理发师（2026-07-12）

#### 本步改了什么？

- 留存提醒卡片现在显示“常用理发师”和客户手机号。
- 没有历史预约、暂时没有匹配理发师的客户显示“暂未指定”。

#### 为什么这样改？

后端本来就会根据客户历史预约次数和最近一次到店记录，计算并返回 `stylist_name`。之前只是前端没有展示这个字段，导致员工不知道该把客户交给谁沟通。

#### 和哪些模块有关？

留存引擎负责计算客户是否需要跟进，`ReminderLog` 保存归属理发师，留存提醒接口返回 `stylist_name`，员工工作台负责展示并保留“已联系/忽略”操作。此次没有改变留存规则，也没有自动发送消息。

#### 面试怎么讲？

> 留存提醒不是只告诉员工“谁需要跟进”，还会根据历史预约为客户匹配常用理发师。这样由熟悉客户的理发师继续沟通，能减少重复了解客户需求，也让 AI 只负责发现和分配建议，最终沟通仍由员工完成。

#### 下一步

刷新留存提醒页面，确认每条客户卡片都能看到常用理发师；没有历史偏好的客户显示兜底文案。

### 第 18 步：学徒染发 RAG 知识库扩充（2026-07-12）

#### 本步改了什么？

- 新增学徒染发 SOP 知识：底色和目标色、冷棕判断、校色、染膏和双氧选择、配比记录、用量、发根发中发尾分区、停留时间、加热、漂发、补根补色、白发、受损发、发质差异、过敏测试、染后护理和师傅确认清单。
- 每条学徒知识增加 `keywords`，并在加载文档时放入 metadata，让同一个关键词同时服务于 BM25 和向量检索。
- BM25 对标题和关键词做适度加权，用户说“橙黄色怎么校色”时会优先命中“橙黄色底色如何校色”，而不是泛泛的染发护理。
- BM25 过滤中文单字噪声，避免“受损发能不能染”被常见的“发”字带偏，优先命中“受损发的染发处理”。
- 员工只读 Graph 增加染膏、双氧、底色、配比、校色、漂粉、加热、冷棕、发根和发尾等学徒问题识别词，确保问题进入 RAG 节点。
- 最后的关键词兜底也会读取 `keywords`，即使向量服务和 BM25 不可用，仍能利用这些业务术语召回内容。

#### 为什么这样改？

RAG 的效果不只取决于文档数量，还取决于三个环节：知识是否覆盖问题、检索是否看得到同义词、Agent 是否把问题路由到知识库。此次同时补齐这三层，解决“知识写了但搜不到”和“员工助手没有进入 RAG”的问题。

#### 和哪些模块有关？

`backend/rag/knowledge_base.py` 提供统一知识源，`backend/rag/bm25_index.py` 负责无 API key 时的本地检索，`backend/rag/retriever.py` 继续保持 Chroma 优先、BM25 兜底，`backend/agents/staff_graph.py` 负责员工问题路由，前端仍通过原有员工助手 API 获取回答。

#### 安全边界

配比、双氧浓度、停留时间和加热条件不能脱离品牌说明书直接给固定数字。知识库明确要求先确认品牌、底色、发质、发束测试和过敏测试，漂发、受损发和异常反应必须由师傅确认；RAG 是学习和查资料辅助，不替代现场判断。

#### 验证方式

- 五个典型问题均能召回对应第一条知识：七度底色冷棕、染膏和双氧配比、发根发尾区别、橙黄色校色、染发加热。
- `pytest -q tests/test_rag_retrieval.py tests/test_staff_graph.py`：21 passed。
- 增加了 Agent 路由测试，确认这些问题经过员工助手 API 时包含 `tool:search_knowledge`。

#### 面试怎么讲？

> 我没有只增加长篇文本，而是把每条知识拆成主题、分类和关键词。检索时标题和关键词权重更高，正文用于补充上下文；员工 Agent 先做意图路由，确认染发问题后再调用 RAG。这样可以同时提高召回率和排序准确性，而且把品牌差异和师傅复核写进知识内容，避免把一个品牌的配方误当成通用规则。

#### 下一步

运行全量测试、编译检查和 `git diff --check`，确认这次 RAG 改动没有影响预约、留存、权限和其他 Agent 流程。

### 第 19 步：Agent 评测方案与生产安全检查（2026-07-13）

#### 本步改了什么

1. 新增 `docs/Agent评测方案.md`，把员工查询、RAG、人工确认、并发抢占和失败追踪整理成固定评测场景。
2. 配置新增 `APP_ENV`、`CORS_ALLOWED_ORIGINS`，并在生产启动时检查 DEBUG、JWT 密钥、跨域白名单和数据库连接。
3. CORS 继续支持跨域，但从固定的全开放配置改为读取白名单；开发环境默认保留本机地址，现有演示方式不受影响。
4. 新增安全配置测试，覆盖合法生产配置、演示配置拒绝和开发模式兼容。

#### 为什么改

面试官不仅会问“Agent 能不能回答”，还会问“怎么证明它可靠”和“演示配置能不能误进生产”。这一步把评测标准写下来，并让应用在生产启动时主动拦截明显危险配置。

#### 和哪些模块有关

- `backend/config.py`：集中读取环境变量并执行生产安全检查。
- `backend/main.py`：应用启动时调用安全检查，并把 CORS 接到白名单配置。
- `tests/test_config_security.py`：验证安全规则不会只停留在文档里。
- `docs/Agent评测方案.md`：记录后续 Agent 回归测试和面试解释。

#### 如何验证

- `python -m py_compile backend/config.py backend/main.py`：通过。
- 使用合法生产参数调用 `validate_security_settings`：通过。
- `pytest -q tests/test_config_security.py`：应全部通过。
- `git diff --check`：确认没有新增空白符错误。

#### 面试怎么讲

> 我把 Agent 评估拆成路由、工具、RAG、安全和可追踪五类，不只看回答是否通顺。配置方面，开发环境保留 SQLite 和本机跨域，生产环境启动时强制检查随机 JWT 密钥、明确 CORS 白名单、关闭 DEBUG，并要求正式数据库连接，避免演示默认值直接上线。

#### 下一步

继续加强预约创建的并发保护：让“抢占时间槽”成为数据库条件更新，两个请求同时到达时最多只有一个能成功。
### 第 20 步：预约时间槽并发保护（2026-07-13）

#### 本步改了什么

1. `AppointmentService.create_appointment` 在真正创建预约前，使用 `UPDATE ... WHERE is_booked = false` 抢占时间槽。
2. 只有数据库实际更新了 1 行，当前请求才继续创建预约；更新 0 行就说明时间槽已经被其他请求抢走，直接返回失败。
3. 保留原来的服务方法参数、返回值和客户端错误分支；同时增加发型师归属校验，避免拿 A 发型师的时间槽创建成 B 发型师的预约。
4. `TimeSlotService.book_time_slot` 也使用同样的条件更新，统一时间槽占用规则。
5. 新增两个测试：同一个时间槽第二次预约失败，以及发型师和时间槽不匹配时不创建预约。

#### 为什么改

原来是“先查询是否空闲，再修改为已预约”两个动作。两个请求同时进来时，可能都在查询阶段看到空闲，造成重复预约。条件更新把“判断和占用”交给数据库完成，数据库只允许一个请求成功抢到这一行。

#### 和哪些模块有关

- `backend/database/service.py`：预约业务的事务边界和时间槽占用。
- `backend/client/appointment.py`：继续把服务层返回的 `None` 转成原有失败响应。
- `backend/database/models.py`：`StylistTimeSlot.is_booked` 和预约外键仍是状态来源。
- `tests/test_booking_concurrency.py`：验证并发边界的业务结果。

#### 如何验证

- `python -m py_compile backend/database/service.py`：通过。
- `pytest -q tests/test_booking_concurrency.py`：`2 passed`。
- 后续全量测试还要验证预约、取消、预约调整和端到端通知没有回归。

#### 面试怎么讲

> 预约抢 slot 不能只依赖应用层先查再改，因为并发请求会产生竞态。我让数据库执行带 `is_booked=false` 条件的原子更新，并根据受影响行数判断是否抢占成功；成功后在同一个事务里创建预约，失败请求不会产生预约记录。这样 SQLite 演示和 PostgreSQL 正式环境都沿用同一个业务规则。

#### 下一步

给员工 Agent 和留存、预约调整 Agent 增加统一 trace：每个任务生成 trace_id，记录意图、工具节点、结果状态，并继续保留现有任务结果字段。
### 第 21 步：Agent trace 链路记录（2026-07-13）

#### 本步改了什么

1. 新增 `backend/agents/trace.py`，统一生成 `trace_id`、记录工作流名称、节点名称、状态、时间和少量安全的节点详情。
2. 员工只读 Graph 记录“意图识别 -> 工具查询 -> 回答整理”。
3. 留存 Graph 记录“收集候选 -> 客户分层 -> 生成建议”。
4. 预约调整 Graph 记录“生成方案 -> 人工确认门 -> 执行写操作”；跨确认阶段还保留 `parent_trace`，方便回看完整过程。
5. API 响应和 `AgentTaskState.result_payload` 增加 trace 信息，但原来的 `actions`、`sources`、proposal 和业务结果字段仍保留。
6. 新增员工、留存和真实员工 API 的 trace 测试。

#### 为什么改

以前虽然有 `actions` 和 `sources`，但它们更像结果摘要，不能完整说明一个任务经过了哪些节点。统一 trace 后，出现错误时可以先按 `trace_id` 找到同一次请求，再判断是路由、工具、人工确认还是最终整理出了问题。

#### 和哪些模块有关

- `backend/agents/trace.py`：统一 trace 和结构化日志。
- `backend/agents/staff_graph.py`、`retention_graph.py`、`appointment_change_graph.py`：在已有节点上追加链路事件。
- `backend/api/routers.py`、`backend/api/schemas.py`：保存并返回 trace，兼容原业务字段。
- `tests/test_agent_trace.py`、`tests/test_staff_graph.py`：验证节点顺序和任务持久化结果。

#### 如何验证

- `python -m py_compile` 检查四个 Agent 模块和 API 模块：通过。
- `pytest -q tests/test_staff_graph.py tests/test_agent_trace.py tests/test_end_to_end_agent_flow.py`：`17 passed`。
- 全量测试还要继续验证钱包、权限、留存和迁移等不直接相关模块。

#### 面试怎么讲

> 我把 Agent 的可观测性拆成两层：`actions` 和 `sources` 给业务接口快速展示，结构化 trace 给排错和审计使用。每次请求有唯一 trace_id，节点只记录意图、工具名、结果数量等非敏感信息；预约调整还把人工确认前后的链路关联起来。这样既不暴露客户原始问题，也能定位一次 Agent 请求到底在哪一步出错。

#### 下一步

跑全量回归和最终代码自审；确认这轮新增的四项能力没有破坏原有预约、权限、钱包、RAG 和前端接口。
### 第 22 步：API 限流与统一错误响应（2026-07-13）

#### 本步改了什么

1. 新增请求 ID 中间件：每个请求都会有 `X-Request-ID`，客户端传入的合法 ID 会原样回传，方便把接口响应和日志对应起来。
2. 新增 API 限流中间件：只限制 `/api` 路径，按客户端 IP 在滑动时间窗口内计数，超过上限返回 429、`Retry-After` 和统一错误结构。
3. 新增统一错误处理：参数校验、HTTP 异常和未知异常都返回 `code`、`message`、`request_id`，未知异常不把堆栈和内部信息返回给用户。
4. 新增限流器和错误响应测试；开发环境默认关闭限流，生产环境安全检查要求显式开启。

#### 为什么改

没有限流时，登录、聊天和 Agent 接口容易被重复请求打满；错误格式不统一时，前端要为每个接口写不同的处理方式。请求 ID 和统一错误结构也让排错更容易。

#### 和哪些模块有关

- `backend/middleware.py`：请求 ID 和进程内限流。
- `backend/api/errors.py`：统一错误响应。
- `backend/main.py`：注册中间件和异常处理器。
- `backend/config.py`：限流开关、窗口和次数配置。
- `tests/test_api_controls.py`：验证限流边界、参数错误、404 和请求 ID。

#### 重要边界

当前限流器是进程内实现，适合面试演示和单进程部署；多 worker 或多副本正式环境需要把计数器替换成 Redis 等共享存储。配置项已经独立出来，后续可以在不改业务路由的情况下替换实现。

#### 如何验证

- `pytest -q tests/test_api_controls.py`：`3 passed`。
- 发送无效登录参数，响应包含 `VALIDATION_ERROR` 和 `request_id`。
- 访问不存在的 API 路径，响应包含 `HTTP_404`、原始错误文案和 `request_id`。

#### 面试怎么讲

> 我在 API 入口做了两层基础治理：先生成 request_id，再对 API 做可配置限流；异常统一转换成 code、message、request_id，未知异常只记录服务端日志，不把堆栈泄漏给用户。限流当前是单进程实现，正式多实例会换成 Redis 共享计数，这是我明确知道的部署边界。

#### 下一步

新增 Dockerfile、Compose 和 Docker 忽略文件，让 PostgreSQL、迁移和 FastAPI 的启动链路可以按同一套配置部署。
### 第 23 步：Docker 部署文件（2026-07-13）

#### 本步改了什么

1. 新增 `Dockerfile`，安装项目依赖，执行数据库迁移后启动 Uvicorn。
2. 新增 `docker-compose.yml`，编排 PostgreSQL 和应用容器，并等待数据库健康检查通过后再启动应用。
3. 新增 `.dockerignore`，避免把 `.env`、演示数据库、缓存和本地向量数据打进镜像。
4. 在 `docs/运行与部署.md` 增加 Docker 启动、停止和生产密钥说明。

#### 为什么改

之前项目可以本地启动，但面试官继续追问“怎么部署到服务器”时，数据库、迁移和应用启动顺序还需要手动解释。Compose 把这些依赖写成可执行配置，降低环境不一致问题。

#### 和哪些模块有关

- `Dockerfile`：应用镜像和启动命令。
- `docker-compose.yml`：PostgreSQL、应用、健康检查和环境变量。
- `backend/config.py`：生产环境安全检查会校验 Compose 传入的密钥、数据库和限流开关。
- `docs/运行与部署.md`：给出实际运行命令和已知边界。

#### 如何验证

- `docker compose -p hair-salon config`：通过，说明 YAML 和环境变量展开正确。
- 未直接启动容器，也没有下载镜像；运行环境若需要实际部署，再执行文档中的 `docker compose up --build`。

#### 面试怎么讲

> 我把应用和 PostgreSQL 用 Compose 编排，应用启动前先跑 Alembic 迁移，并通过数据库 healthcheck 控制启动顺序。敏感密钥通过环境变量传入，`.dockerignore` 防止本地数据库和 `.env` 进入镜像。当前限流器是单进程实现，多副本部署前还要换共享存储。

#### 下一步

补齐员工操作审计：在现有 `AuditLog` 基础上记录 Agent 查询、预约调整和留存工作台操作，并用接口测试验证操作者和实体 ID。
### 第 24 步：员工操作审计增强（2026-07-13）

#### 本步改了什么

1. 员工只读 Agent 查询成功后记录 `agent.staff_query_completed`，保存操作者、任务 ID、意图、动作和 trace_id。
2. 预约调整记录提议、拒绝和确认三个阶段，分别对应 `agent.appointment_change_proposed`、`agent.appointment_change_rejected`、`agent.appointment_change_confirmed`。
3. 留存扫描和留存 Agent 完成后记录审计；员工忽略提醒时记录 `retention.reminder_dismissed`。
4. `RetentionService.dismiss` 增加可选操作者参数，保留原来不传操作者时的调用兼容性。
5. 新增两个接口级测试，验证审计日志中的 actor_user_id、entity_id 和 trace 信息。

#### 为什么改

原来部分业务写操作有审计，但员工 Agent 查询和留存“忽略”没有留下完整的操作者和实体关联。补齐后，审计台不仅能回答“数据库发生了什么”，还可以回答“哪个员工通过哪个任务做了什么”。

#### 和哪些模块有关

- `backend/database/finance.py`：继续复用已有 `FinanceService.create_audit`。
- `backend/api/routers.py`：在 Agent、留存扫描和员工操作成功后写入审计。
- `backend/database/retention.py`：记录忽略提醒的员工。
- `backend/database/models.py`：继续使用现有 `AuditLog` 表，不新增迁移。
- `tests/test_audit_enhancement.py`：验证审计操作者和实体关系。

#### 如何验证

- `pytest -q tests/test_audit_enhancement.py`：`2 passed`。
- 通过员工 Agent 查询后，`GET /api/audit-logs` 能找到对应 task_id 和 trace_id。
- 员工忽略提醒后，审计记录的 entity_id 等于 reminder_id。

#### 面试怎么讲

> 我复用了统一 AuditLog，而不是每个模块各造一套日志。所有关键员工动作都记录 actor、action、entity_type、entity_id 和结构化 details；Agent 额外带 trace_id，能从业务审计跳到工作流节点。这样权限、业务结果和可追溯性是连在一起的。

#### 下一步

完成第二轮全量回归、静态检查和最终工作区审计；确认新增文件、已有接口和部署配置都处于可交付状态。
### 第 25 步：第二轮全量回归与最终自审（2026-07-13）

#### 本步改了什么

- 修复了统一错误处理对 Starlette 404 异常的覆盖边界。
- 修复了合法生产配置测试遗漏限流开关的问题。
- 修复了环境模板中的字面量换行符，确保 `RATE_LIMIT_ENABLED` 等变量复制后能被正确读取。
- 对本轮所有新增和接入点做了全量回归、编译、Compose 和 diff 检查。

#### 最终验证结果

- `pytest -q`：`70 passed`，只有 1 条已有的 Starlette/httpx 弃用警告。
- `python -m compileall -q backend migrations tests`：通过。
- `docker compose -p hair-salon config`：通过；中文目录下不指定项目名时 Docker Compose 会报项目名为空，所以验证命令显式指定了项目名。
- `git diff --check`：没有新增空白符错误。
- `git diff --diff-filter=D --name-only`：没有删除文件。

#### 最终面试复述

> 这轮我没有只堆功能，而是补了四类工程能力：Agent 评测和 trace 让 AI 行为可验证、可定位；生产配置检查和统一错误让接口更接近真实部署；预约使用数据库条件更新解决并发抢 slot；Docker、限流和审计补上部署、稳定性和责任追踪。原有预约、权限、钱包、RAG、留存和前端接口继续保留，并用全量测试验证没有回归。

#### 当前交付边界

- 本地演示仍然使用 SQLite，生产 Docker Compose 使用 PostgreSQL。
- 当前限流器是单进程内存实现，多 worker 或多副本部署前需要替换成 Redis 等共享计数器。
- 本轮没有实现第三方支付/短信，也没有开始前端工程化升级，按之前约定留到最后。

#### 下一步

本轮 1、2、3、4、6、7、8、10 已完成；后续再做 9 前端工程化升级和 11 第三方支付/消息集成。

### 第 26 步：修复本地数据库迁移和前端错误提示（2026-07-13）

#### 本步改了什么

- 执行 `alembic upgrade head`，把本地 `hair_salon.db` 从 `0004` 升级到 `0005_add_member_expiry`，补齐 `members.expires_at` 字段。
- 修复客户端错误解析：后端统一错误响应使用 `message` 字段时，前端现在会优先展示 `message`，不再只显示笼统的 `HTTP 500`。

#### 为什么改

注册接口本身已经成功，但注册后页面会加载会员信息；旧数据库缺少新字段，导致这个后续请求返回 500。用户再次注册同一手机号就会得到 409，看起来像注册失败，实际是第一次已经注册成功。

#### 如何验证

- `alembic current`：`0005_add_member_expiry (head)`。
- `pytest -q tests/test_auth.py tests/test_customer_api.py`：`7 passed`。
- `/api/health`：返回 `200`。

#### 面试怎么讲

> 这次问题不是简单重试能解决的，而是应用模型和本地数据库版本不一致。通过 Alembic 把数据库升级到迁移头，保留已有用户数据；同时让前端兼容统一错误响应的 message 字段，避免把后端真实原因隐藏成 HTTP 状态码。

#### 下一步

刷新客户端页面，使用已经注册成功的手机号和密码登录，不要再次注册同一个手机号。

### 第 27 步：退出登录与用户友好错误提示（2026-07-13）

#### 本步改了什么

- 在 H5 “我的”页面的隐私政策入口下方增加独立的“退出登录”模块，隐私政策详情页不放退出按钮；按钮复用已有的 `logout()` 方法。
- 退出登录时删除浏览器保存的 JWT，清空当前用户状态，并返回登录主页。
- 增加统一错误提示映射：登录失败显示“账号或密码错误”，未登录或 Token 失效显示“登录已失效”，无权限显示“当前账号没有权限”，用户不存在显示“用户不存在”，重复注册显示“用户已存在，请直接登录”，参数错误和服务器错误也显示对应的中文提示。
- 后端仍然返回标准的 HTTP 状态码，前端只负责把状态码翻译成用户能理解的信息。

#### 为什么改

用户直接看到 `401`、`403`、`409` 或 `500` 时，很难判断下一步该怎么做。把状态码保留给程序和日志，把提示翻译给用户，可以同时满足接口规范和实际使用体验。退出登录则让用户能够主动结束当前会话，方便更换账号和重新验证注册、登录流程。

#### 和其他模块的关系

- `frontend/index.html` 的 `apiRequest()` 是 H5 所有 API 请求的公共入口，因此错误提示可以集中处理，避免每个页面重复写一遍状态码判断。
- 隐私政策页和个人资料页共用现有 `logout()`，保证不同入口退出后的行为一致。
- JWT 仍由后端验证；前端删除 Token 只是结束本地会话，不能替代后端鉴权。

#### 如何验证

- 浏览器登录后在“我的”页面确认隐私政策下面有独立的“退出登录”模块；点击后确认回到登录页且不能继续访问需要登录的页面。
- 打开隐私政策详情页，确认页面只保留隐私政策内容和返回入口。
- 使用错误密码，确认显示“账号或密码错误”。
- 使用已注册手机号注册，确认显示“用户已存在，请直接登录”。
- 未登录访问受保护接口或使用失效 Token，确认显示“登录已失效，请重新登录”。
- 运行前端 JavaScript 语法检查和后端回归测试。

#### 面试怎么讲

> 我把前端 API 请求统一收口到 `apiRequest()`，根据 HTTP 状态码做用户提示映射，同时保留后端状态码作为程序层面的标准契约。退出登录复用统一的会话清理逻辑，删除 JWT 并清空内存状态，避免不同页面出现退出行为不一致的问题。

#### 下一步

继续进行浏览器回归和最终工作区检查；第三方支付、消息集成和前端工程化升级仍按之前约定留到最后。

### 第 28 步：同步小程序退出登录与错误提示（2026-07-13）

#### 本步改了什么

- 在小程序“我的”页面的隐私政策入口下方增加独立的“退出登录”按钮。
- 退出按钮复用小程序已有的 `app.logout()`，清除本地 JWT、清空当前会话状态并跳转到登录页。
- 在小程序统一 `app.request()` 中补齐 `401/403/404/409/422/500` 错误提示，并兼容后端统一错误响应中的 `message` 字段。
- 小程序隐私政策详情页、后端、数据库和其他业务页面没有修改。

#### 为什么改

H5 和小程序共用后端接口，如果只有 H5 能看到易懂的错误提示，两个客户端的使用体验和演示结果会不一致。把转换逻辑放在小程序统一请求入口，可以让登录、预约、充值和通知等页面自动复用同一套提示。

#### 如何验证

- 检查小程序“我的”页面中隐私政策下面存在独立的退出登录按钮。
- 检查隐私政策详情页没有退出登录按钮。
- 使用错误密码、重复注册和失效 Token，确认分别显示账号密码错误、用户已存在和登录失效提示。
- 运行小程序全部 JavaScript 语法检查和 JSON 解析检查。

#### 面试怎么讲

> H5 和小程序虽然页面技术不同，但都通过统一请求层处理认证和错误。小程序复用已有的 logout 会话清理逻辑，并在 request 封装中把后端标准状态码翻译成用户提示，避免每个页面重复处理 401、403 和 409。

#### 下一步

保存本次小程序同步后的版本，并继续进行最终工作区检查。

### 第 29 步：修复注册后 500 并增加微信登录（2026-07-13）

#### 本步改了什么

- 根据小程序截图和服务日志定位到：手机号注册接口已经成功创建用户，真正失败的是注册后读取 `/api/members`；本地数据库虽然记录在 `0005` 迁移头，但实际缺少 `members.expires_at` 字段。
- 新增 `0006_repair_member_expiry`，只在字段缺失时补列，不删除或重建已有业务数据；本地数据库已升级到最新版本。
- 新增 `users.wechat_openid`，并允许微信首次登录用户暂时没有手机号，不生成假手机号。
- 新增 `POST /api/auth/wechat`：后端使用微信小程序传来的临时 code 调用微信 `jscode2session`，拿到 openid 后查询或创建客户并签发现有 JWT。
- 小程序登录页增加“微信登录”按钮，成功后复用现有用户加载和“我的”页面流程。
- 微信 AppID 和 AppSecret 只从环境变量读取；未配置时返回明确提示，不会伪造登录成功。
- 没有删除手机号注册/登录、预约、钱包、通知或其他小程序页面。

#### 微信登录流程

```text
小程序点击微信登录
  -> wx.login 获取临时 code
  -> POST /api/auth/wechat
  -> 后端使用 AppID + AppSecret 向微信换取 openid
  -> 查询或创建 users.wechat_openid
  -> 签发现有 JWT
  -> 小程序保存 JWT，加载用户数据并进入“我的”
```

微信首次登录没有手机号授权，因此预约接口会要求先绑定手机号；这样不会把 openid 伪装成手机号，后续可以单独补手机号绑定流程。

#### 如何验证

- `alembic upgrade head`：实际演示数据库升级到 `0007_add_wechat_auth`。
- SQLite 检查确认 `members.expires_at`、`users.wechat_openid` 存在，`users.phone` 可以为空。
- 微信登录模拟测试覆盖首次创建用户、重复登录复用用户和缺少 code 的参数校验。
- `pytest -q`：`72 passed`，只有已有的 Starlette/httpx 弃用警告。
- 小程序 14 个 JavaScript 文件语法检查通过，17 个 JSON 文件解析通过。
- `/api/health`：返回 `200`，Uvicorn 已重新启动。
- `git diff --check`：没有新增空白符错误。

#### 真实环境边界

微信登录要在真实设备工作，还需要在微信公众平台配置 AppID、AppSecret 和合法的 HTTPS request 域名；本地 `127.0.0.1` 只能完成代码和接口验证，不能代替微信平台配置。

#### 面试怎么讲

> 我没有把微信登录做成前端直接相信用户信息，而是让小程序只提交一次性 code，后端再向微信官方换取 openid，并用 openid 关联本地用户、签发统一 JWT。手机号是独立的可选绑定信息，避免把第三方身份标识伪装成业务手机号。数据库迁移还增加了幂等修复逻辑，解决迁移版本号和实际表结构不一致导致的注册后 500。

#### 下一步

配置真实微信 AppID、AppSecret 和 HTTPS 域名后，在微信开发者工具或真机验证微信登录；手机号绑定流程仍可作为后续增强。

### 第 30 步：统一 H5 与小程序老师展示（2026-07-13）

#### 本步改了什么

- H5 和小程序都改为使用老师手机号作为稳定映射键，不再根据接口数组下标分配头像。
- 两端都使用同一组展示顺序：张三、李四、王五、赵六。
- 两端都使用小程序目录中的同一组 `*-portrait.png` 头像资源，H5 通过静态资源路径读取同一份文件。
- 没有修改发型师接口、老师 ID、时间槽、预约逻辑或数据库数据。

#### 为什么改

以前 H5 和小程序虽然都取 `/api/stylists`，但 H5 的头像数组从 `sophie-bob.png` 开始，小程序从 `chen-yu-portrait.png` 开始；同一个数组位置因此对应了不同头像。只按数组下标绑定还有另一个问题：后端返回顺序变化后，老师和头像可能错配。

#### 如何验证

- 检查两端都按手机号映射张三、李四、王五、赵六。
- 检查 H5 和小程序使用相同文件名的头像资源。
- 运行 H5 脚本语法检查和小程序 JS/JSON 检查。
- `git diff --check` 通过，确认没有删除文件或修改后端预约模块。

#### 面试怎么讲

> 展示层不能把数组下标当成业务身份。我用后端稳定的老师手机号做映射键，两端共享同一组排序和头像文件；这样即使接口返回顺序变化，老师、头像和位置仍然保持一致。

#### 下一步

在 H5 和微信开发者工具中分别打开预约页面，目视确认四位老师的顺序、姓名和头像完全一致。

### 第 31 步：补充员工端经营统计（2026-07-13）

#### 本步改了什么

- 员工“客户与会员”增加当前余额、累计充值和账户流水查看；流水包含充值、退款、消费和调整后的余额。
- 员工工作概览增加今日营业数据：消费、充值、已处理退款、待审核退款、消费客户数、订单数和套餐分布。
- 增加“员工管理”板块，按发型师展示今日服务客户、服务套餐、订单数、客户数和业绩金额。
- 员工管理会展示所有在职发型师，即使当天没有消费也会显示 0 业绩，避免员工在列表中消失。
- 更新员工端脚本缓存版本，确保浏览器能加载本次最新页面。
- 新增员工专用只读接口 `/api/staff/customer-wallets` 和 `/api/staff/overview`，继续复用现有钱包、退款、消费和预约数据。
- 没有新增数据库表，也没有修改顾客端、小程序和原有充值退款写入逻辑。

#### 统计口径

- 消费金额来自当天创建的 `transactions`，套餐统计按消费记录的 `service` 字段分组。
- 充值金额来自当天钱包流水中类型为 `recharge` 的入账记录。
- 退款金额只统计已经审核通过并生成钱包扣减流水的退款；待审核金额单独展示。
- 发型师业绩通过消费记录关联的预约和发型师归属；没有关联预约的消费会显示为“未关联发型师”，避免金额消失。

#### 如何验证

- `tests/test_staff_overview.py`：覆盖员工查看客户余额、充值记录、今日营业统计、发型师业绩归属和普通客户权限隔离，`3 passed`。
- `python -m compileall -q backend`：通过。
- `staff.js` JavaScript 语法检查：通过。
- 全量 `pytest -q`：`75 passed`，只有已有的 Starlette/httpx 弃用警告。
- `git diff --check`：通过。

#### 面试怎么讲

> 我把员工端统计建立在已有业务流水之上，而不是根据页面上的预约数量猜营业额。消费来自交易表，充值和退款来自钱包流水，发型师业绩通过预约关系归属；接口只读并受员工权限保护，因此统计和业务写入是解耦的。

#### 下一步

在员工工作台登录后检查客户流水、今日营业额和员工管理页面的视觉效果；确认无误后再保存当前版本。

### 第 32 步：员工端页面验收（2026-07-13）

#### 本步确认了什么

- 工作概览首页只保留今日预约、会员客户、待处理退款和待跟进客户的数量入口，以及今日营业额统计。
- 点击“今日预约”后，仍能进入完整预约列表；点击“留存提醒”后，仍能进入待跟进客户列表。
- 员工管理只展示和 H5 一致的四位发型师：张三、李四、王五、赵六，并保持这个顺序。
- 浏览器确认已经加载 `staff.js?v=20260713-2`，不是旧缓存版本。

#### 如何验证

- 浏览器打开 `/staff?reload=20260713-2`，确认工作概览没有直接展开预约和留存明细。
- 点击“今日预约”，看到当天按发型师分组的预约记录。
- 点击“留存提醒”，看到待跟进客户及“已联系/忽略”操作。
- 点击“员工管理”，确认页面显示 4 个服务分组，且只有张三、李四、王五、赵六。

#### 面试怎么讲

> 我把工作概览定位成信息入口和经营摘要，避免首页信息过载；详细数据仍然保留在对应业务模块中。员工管理使用和 H5 相同的稳定手机号映射，只展示实际业务范围内的四位发型师，保证两端展示一致。

#### 下一步

全量测试和静态检查通过后，保存当前版本。

### 第 33 步：员工工作台 UI 风格重构（2026-07-13）

#### 本步改了什么

- 参考 `meoo.com/explore/170468` 中企业级工作台的视觉语言，将员工端改为深色侧边导航、蓝色当前模块、浅色工作区和白色内容卡片。
- 统一了工作概览指标卡、营业额面板、预约分组、员工绩效、提醒、表格和按钮的颜色、圆角、边框、阴影和间距。
- 保留了原有的工作概览、今日预约、客户与会员、员工管理、退款处理、留存提醒、操作审计和员工助手，不改变接口、数据库、权限和点击行为。
- 为 `staff.css` 和 `staff.js` 更新静态资源版本号，避免浏览器继续使用旧缓存。

#### 为什么这样改

参考页面采用的是企业运营后台常见的信息层级：深色导航负责定位模块，蓝色高亮表示当前页面，浅色背景降低视觉噪音，白色卡片承载具体数据。员工端本身也是运营工作台，因此沿用这种结构比继续使用偏门店展示感的绿色界面更适合日常工作和面试演示。

#### 如何验证

- 浏览器桌面端打开 `/staff?reload=20260713-3b`，确认首页、营业额卡片和导航样式已更新。
- 切换到“员工管理”，确认四位发型师、业绩金额和明细表格仍正常显示。
- `pytest -q tests/test_staff_overview.py tests/test_staff_web.py`：`4 passed`。
- `node --check frontend/staff.js`、`python -m compileall -q backend` 和 `git diff --check`：通过。
- `/api/health`：返回 `200`。

#### 面试怎么讲

> 这次改动严格限定在展示层，采用企业后台常见的导航和卡片化信息层级。后端接口、数据库和业务操作没有变化，所以可以在不增加业务风险的前提下提升可读性和专业度；同时通过静态资源版本号解决浏览器缓存导致的旧样式问题。

#### 下一步

保存当前版本；后续如果继续优化，可以再补充统一的 SVG 图标库和更细的移动端交互，但不应改变当前业务模块边界。

### 第 34 步：套餐服务核验与员工绩效闭环（2026-07-13）

#### 本步改了什么

- 新增服务套餐目录、客户套餐账户和服务核验记录三类数据。
- 员工可以在“今日预约”中点击“核验服务”，查看客户匹配的有效套餐、剩余次数和到期时间。
- 核验阶段只创建 `verified` 记录；点击“确认完成服务”后，才会扣减套餐次数、生成消费交易、把预约改为已完成，并计入发型师当天绩效。
- 套餐服务金额按套餐总价除以总次数计算，直接消费则由员工录入本次实际金额。
- 完成服务接口具有幂等性，重复点击不会重复扣次数或重复生成消费。
- 客户不能再通过旧消费接口携带预约 ID 自己生成交易，预约关联消费必须经过员工核验。
- 新增数据库迁移 `0008_add_service_verification`，并已升级当前开发库。

#### 关键接口

- `GET /api/staff/service-packages`：查看可用套餐目录。
- `POST /api/staff/customer-packages`：为客户录入已购买的套餐权益。
- `GET /api/staff/appointments/{appointment_id}/verification`：读取预约和可用套餐。
- `POST /api/staff/appointments/{appointment_id}/verify`：员工核验服务。
- `POST /api/staff/service-verifications/{verification_id}/complete`：确认服务完成并计入绩效。

#### 如何验证

- 端到端测试覆盖创建套餐、绑定客户、预约、核验、扣次、生成交易、绩效统计和重复完成保护。
- `pytest -q tests/test_staff_overview.py tests/test_migrations.py`：`5 passed`。
- 浏览器打开员工端，在“今日预约”点击“核验服务”，确认核验面板显示客户、服务、套餐数量、金额输入和完成说明。
- 当前开发数据库已执行 `alembic upgrade head`，服务健康检查返回 `200`，OpenAPI 已包含核验接口。

#### 面试怎么讲

> 我把预约、套餐权益、服务核验和消费交易拆成不同状态。预约只能代表客户预订，套餐账户记录可用次数，员工核验确认服务真实发生后，后端在一次事务中扣次数、写核销、写消费并更新绩效；通过唯一预约核验记录和完成状态保证重复点击不会重复扣费。

#### 下一步

补充套餐目录和客户套餐录入的可视化管理页面，并在真实支付接入后把“套餐权益录入”替换为支付成功后的自动发放。

### 第 35 步：客户列表限制高度并支持内部滚动（2026-07-13）

#### 本步改了什么

- 给“客户与会员”列表增加独立滚动窗口，最大高度按视口高度的 56% 计算，并设置 520px 上限。
- 客户超过可视区域后只在表格内部上下滚动，同时保留小屏幕下的横向滚动，不再把整个页面无限撑长。
- 表头使用 sticky 固定，滚动客户数据时仍能看到字段名称。
- 更新 `staff.html` 的静态资源版本号，避免浏览器继续使用旧的 CSS 和 JavaScript 缓存。

#### 为什么这样改

客户数量可能从几个人增长到几百甚至上千人。如果让表格自然向下展开，用户需要滚动很长的页面，页面其他区域也会被推得很远。限制表格容器高度可以把列表变成一个可控的数据窗口，既保持页面比例，也方便连续查看客户；这次只改前端容器，不改变客户查询接口和业务数据。

#### 如何验证

- 在员工工作台进入“客户与会员”，确认少量客户时表格正常显示。
- 准备较多客户数据后，确认表格达到最大高度后出现内部滚动条，页面整体高度不再持续增长。
- 滚动表格时确认表头保持在顶部；在窄屏幕上确认可以横向滚动查看完整字段。
- `node --check frontend/staff.js` 和 `git diff --check`：通过。

#### 面试怎么讲

> 客户列表采用容器级滚动，把最大高度和视口比例绑定，避免数据量增长导致整个页面无限变长；同时固定表头，保证长列表浏览时字段语义不会丢失。后端接口保持不变，展示层可以独立优化。

#### 下一步

如果客户规模长期达到几千或几万条，再进一步增加分页或虚拟列表，减少一次性渲染的 DOM 数量。

### 第 36 步：移除会员到期分析并增强留存依据（2026-07-13）

#### 本步改了什么

- 移除了“运行运营分析”中的“会员到期”分支和统计卡，保留客户与会员页面的基础会员数据。
- 将运营分析的流失判断统一到现有留存引擎：优先使用客户历史到店间隔计算个人复购周期，再用个人周期的 2.5 倍判断流失风险。
- 余额客户仍按账户余额大于 0 判断，但会同时返回余额证据。
- 分析结果新增扫描客户数、数据来源、判断规则和每个客户的命中依据，员工可以看到为什么推荐联系某位客户。
- 更新留存分析接口、前端展示、测试和静态资源版本号。

#### 为什么这样改

原来的 Agent 使用固定 60 天阈值，并额外判断会员到期，和系统已有的“按个人到店节奏留存”规则不一致，分析结果显得简单且不容易解释。现在 Agent 复用同一套个人节奏规则，保证“留存提醒”和“运营分析”对风险的理解一致；同时把规则和证据展示出来，避免把结果包装成无法追溯的黑盒结论。

#### 如何验证

- 在员工工作台运行运营分析，确认结果只显示“流失风险”和“余额客户”两类指标。
- 确认分析依据区域显示扫描客户数、数据来源和判断规则。
- 确认每条分析建议显示个人到店周期、当前未到店天数和风险阈值，或显示账户余额。
- `pytest -q tests/test_retention_agent.py tests/test_agent_trace.py`：通过。
- `node --check frontend/staff.js` 和 `git diff --check`：通过。

#### 面试怎么讲

> 留存 Agent 不直接依赖大模型猜测客户状态，而是编排数据库读取、个人复购周期计算、风险分类和建议生成。个人周期来自历史到店间隔，没有足够历史时再按最近服务类型使用默认周期；最终把命中规则和证据返回给员工，做到可解释、可复盘、可人工确认。

#### 下一步

后续可以继续增加风险分数和趋势对比，例如比较最近三次到店间隔是否持续变长，但需要先积累更多真实客户数据再调整阈值。

### 第 37 步：建立 H5 与小程序统一视觉令牌（2026-07-13）

#### 本步改了什么

- 将 H5 和小程序的基础颜色统一到员工工作台使用的蓝色企业风格：页面浅灰背景、白色卡片、蓝色主按钮、深蓝文字和统一的状态色。
- H5 在 `:root` 中统一颜色、圆角和阴影变量；小程序在 `app.wxss` 的 `page` 中建立对应的 WXSS 变量。
- 统一两端的基础卡片、指标卡、主要按钮、次要按钮、标签和页面背景。
- 更新小程序系统导航栏和 TabBar 的背景色、选中色，保证系统级导航也与页面主色一致。
- 暂时保留 H5 底部导航和小程序原生 TabBar，只同步视觉语言，不强行复制平台不适合的布局。

#### 为什么这样改

H5 和小程序的业务入口相同，但之前使用了多套蓝色、青绿色、米白色和荧光色，用户在两个端之间切换时会感觉像不同产品。先建立相同的设计令牌，可以让后续页面改造有统一依据；同时保留 H5 和小程序各自的导航方式，兼顾品牌一致性和平台使用习惯。

#### 如何验证

- H5 检查页面背景、卡片、按钮和底部导航是否使用新的蓝色设计令牌。
- 小程序检查登录页、我的页面、预约页的基础背景、卡片、按钮和 TabBar 是否使用同一组主色。
- `git diff --check`：通过。

#### 面试怎么讲

> 跨端统一不是复制同一份布局，而是先建立共享设计令牌，再根据 H5 和微信小程序的交互规范适配布局。这样颜色、字体层级、卡片和状态保持一致，同时保留 H5 底部导航和小程序原生 TabBar，减少平台违和感。

#### 下一步

继续按页面逐步替换剩余的旧色值和旧组件样式，优先处理登录、预约、老师排档和我的页面，并在 H5 与小程序中分别做移动端视觉验证。

### 第 38 步：收口跨端样式改动范围（2026-07-13）

#### 本步改了什么

- 根据协作范围要求，回退上一阶段对 H5 和小程序全局样式令牌的改动。
- 保留员工工作台 `staff` 页面本身的 UI、客户列表滚动和留存分析展示改动。
- 不继续修改 H5 客户端和小程序其他页面的视觉样式，避免影响无关页面。

#### 为什么这样改

当前目标是只优化正在确认的这个工作台界面，而不是重做整个 H5 和小程序。回退全局样式可以避免登录、预约、我的页面等其他页面被连带改变，保证改动边界清晰。

#### 如何验证

- 检查 H5 和小程序全局样式文件恢复到本阶段改动前的状态。
- 检查员工工作台 `staff.css`、`staff.js` 和 `staff.html` 的既有改动仍然保留。
- `git diff --check`：通过。

#### 面试怎么讲

> 我把本次 UI 优化限定在员工工作台边界内，避免为了统一视觉而影响客户端其他页面。这样做可以降低回归风险，也方便后续按页面逐步推进，而不是一次性改动整个前端。

#### 下一步

除非重新确认，否则不再修改 H5 和小程序的其他页面。

### 第 39 步：同步 H5 与小程序客户端 UI（2026-07-13）

#### 本步改了什么

- 只修改 H5 的 `frontend/index.html` 样式，以及小程序的 `app.wxss`、页面 `*.wxss` 和 `app.json` 颜色配置。
- 将两端的页面背景、卡片、按钮、指标卡、标签、输入框、预约卡片、时间卡片和导航颜色统一到蓝色企业工作台风格。
- H5 继续使用底部导航，小程序继续使用微信原生 TabBar，只统一视觉语言，不复制不适合平台的布局。
- 没有修改 H5/小程序业务 JavaScript、WXML、后端、API、数据库和员工工作台文件。

#### 为什么这样改

H5 和小程序承载的是同一套客户业务，颜色和组件应该让用户感受到它们属于同一个产品。统一设计令牌可以减少跨端视觉差异；保留各端原生导航方式，则能避免为了“看起来一样”而牺牲平台使用习惯。

#### 如何验证

- H5 检查首页、预约、会员卡、充值和记录页面的基础颜色与卡片样式。
- 小程序检查登录、我的、预约、老师排档、充值、退款和历史页面的基础颜色与卡片样式。
- 检查 H5 和小程序样式文件不再残留主要旧配色。
- `git diff --check`：通过。
- Git 改动范围仅允许 H5/小程序 UI 文件；没有新增后端或员工工作台改动。

#### 面试怎么讲

> 我把跨端统一拆成设计令牌统一和布局适配两部分。H5 与小程序共享颜色、卡片、按钮、间距和状态规范，但分别保留底部导航和原生 TabBar，既保持品牌一致，也符合各自平台的交互习惯。

#### 下一步

如果后续继续优化，只在 H5 和小程序的 UI 文件范围内进行，不扩展到后端或其他业务模块。

### 第 40 步：重排 H5 登录页（2026-07-13）

#### 本步改了什么

- 将 H5 登录页从大面积渐变头图改为居中卡片式布局，品牌、标题和说明文字统一居中。
- 将标题改为单行显示，减少不必要的断行，让“欢迎回来”成为清晰的页面主标题。
- 给姓名、手机号和密码增加明确的字段标签、输入提示、统一高度和聚焦状态。
- 登录按钮保留为唯一主按钮，注册切换改成文字型次级入口。
- 只修改 `frontend/index.html` 的登录页结构和样式，没有修改登录接口、认证逻辑、后端或小程序。

#### 为什么这样改

原来的登录页中，渐变头图、标题和多个按钮同时争夺注意力，输入框也缺少明确的字段层级。居中卡片可以把登录任务集中在一个稳定区域，主按钮和次级入口的层级也更清楚；同时保留原有 `authForm`、元素 ID 和 `data-auth-mode`，因此不会影响已有登录和注册切换逻辑。

#### 如何验证

- 在 H5 首页检查登录页标题、品牌、说明文字和表单是否居中对齐。
- 检查手机号和密码输入框仍然可以填写，登录按钮仍然提交 `authForm`，注册入口仍然切换登录状态。
- 检查窄屏下没有横向滚动，输入框和按钮仍然满足移动端触控尺寸。
- 运行 `git diff --check`，确认没有空白字符错误。

#### 面试怎么讲

> 登录页采用居中卡片式布局，并把操作分成主操作和次级操作：登录是唯一主按钮，注册是文字入口。这样可以降低视觉干扰；同时我保留原有表单 ID 和事件绑定，只改展示层，保证 UI 重构不会影响认证业务。

#### 下一步

打开 H5 首页做一次实际视觉检查；如果整体效果符合预期，再继续处理其他页面的局部排版问题。

### 第 41 步：重排小程序登录页（2026-07-13）

#### 本步改了什么

- 将小程序登录页改为和 H5 对齐的居中卡片式布局：品牌、标题和说明文字居中显示。
- 将姓名、手机号和密码改成带字段标题的输入组，并统一输入框高度、圆角和间距。
- 保留原有手机号登录、注册切换和微信登录功能，只调整 WXML 展示结构与 WXSS 样式。
- 登录按钮作为主操作，注册入口使用文字型次级按钮，微信登录放到“或”分隔线下方作为另一种登录方式。
- 只修改 `frontend/miniprogram/pages/login/login.wxml`、`login.wxss` 和本开发总结，没有修改 `login.js`、接口、后端或数据库。

#### 为什么这样改

小程序原来的标题、表单和三个按钮连续排列，层级不够清楚；同时输入框只有占位文字，用户填写时看不到字段提示。居中卡片和明确标签可以让登录流程更集中，主登录按钮、注册入口和微信登录也能形成清晰的优先级。保留原有事件绑定，确保这次只改变展示层，不影响功能。

#### 如何验证

- 在微信开发者工具中检查登录页的品牌、标题、说明文字和表单是否居中显示。
- 检查手机号登录、注册切换和微信登录按钮仍然绑定 `onInput`、`toggleMode`、`submit` 和 `wechatLogin`。
- 检查注册模式出现姓名字段，登录模式不显示姓名字段。
- 检查小屏手机上没有横向滚动，按钮和输入框仍然方便触摸。
- 运行 WXML/WXSS 静态检查和 `git diff --check`。

#### 面试怎么讲

> 小程序和 H5 统一的是视觉层级，不是强行复制相同代码。两端都采用居中卡片、明确字段标签和单一主操作，但小程序继续使用自己的 WXML 绑定和微信登录能力，这样既保持跨端一致，也符合平台实现方式。

#### 下一步

在微信开发者工具中打开小程序登录页，分别检查登录模式、注册模式和微信登录按钮的显示与交互。

### 第 42 步：员工助手模型接入与余额兜底（2026-07-13）

#### 本步改了什么

- 修复员工工作台接口绕过模型的问题：/api/staff/agent/query 现在优先调用 LangChain 员工 Agent，由模型理解自然语言并自主选择只读工具。
- 给员工 Agent 增加按姓名或手机号查客户、查会员等级/积分/余额、查留存提醒的工具；原有日程、生日会员和护理知识工具继续保留。
- 模型不可用或调用失败时，继续回退到原来的员工只读 Graph，不允许助手直接修改预约、钱包或通知。
- 补充模型入口、余额不足兜底和失败任务审计相关测试，并保留工具动作、数据来源和 trace 信息，方便排查和面试演示。

#### 为什么这样改

之前员工端虽然项目里已经有 LangChain Agent，但工作台实际调用的是固定关键词分类器。它只能判断“预约/会员/护理”等少数关键词，再拼接固定句子，所以看起来不像 AI。现在让模型负责“理解问题和选择工具”，让工具负责“查询真实数据”，形成“模型做决策、业务工具做事实校验”的分工。

本地测试发现当前 .env 的模型调用返回 402 Insufficient Balance，因此运行时会自动进入规则兜底。这说明代码链路已经接通，但要看到真正的自然语言能力，还需要给模型服务补充有效余额或更换可用的兼容 OpenAI API 配置。

#### 如何验证

- 运行 pytest -q tests/test_staff_graph.py tests/test_agent_trace.py tests/test_chat_agent.py，确认模型入口、规则兜底、只读查询和失败任务审计。
- 在员工工作台提问“帮我看看明天的预约，并告诉我最忙的发型师”，有有效模型配置时应由模型选择日程工具并组织回答；模型不可用时仍能返回规则查询结果。
- 查看接口返回的 actions、sources 和 trace，确认回答使用了哪些工具和数据来源。

#### 面试怎么讲

> 员工助手不是让大模型直接访问数据库，而是给它一组只读业务工具。模型负责理解“明天预约、最忙发型师”这类自然语言并选择工具，工具只返回数据库里的真实结果，最后模型负责把结果整理成人能看懂的回答。如果模型服务不可用，系统降级到确定性的员工查询 Graph，保证核心查询仍然可用。

#### 下一步

补充有效的模型服务余额或替换可用的 API 配置，再在员工工作台验证多轮追问、组合查询和护理知识问答；同时继续观察模型是否会在数据不足时主动追问，而不是猜测。

### 第 43 步：补充员工助手演示业务数据（2026-07-14）

#### 本步改了什么

- 新增 `backend/database/seed_demo_customers.py`，使用 SQLAlchemy 只重建手机号以 `1337000` 开头的演示客户，不覆盖原有客户。
- 演示数据按固定随机种子生成，重复执行会先清理这段演示手机号，再重新生成同一批数据，避免重复插入，也方便稳定复现问题。
- 150 位演示客户包含会员资料、充值和消费流水、历史预约及四位发型师的服务记录；到店次数分布为 1 次 7 人、2 次 42 人、3 次 40 人、4 次 40 人、5 次 19 人、6 次 2 人。
- 消费金额平均约 1004.72 元，中位数约 931.95 元；最近到店时间分布为 30 天内 24 人、31—90 天 69 人、91—180 天 46 人、181—365 天 11 人。
- 按要求写入龙百川：手机号 13370000003，充值 1000 元，2026-05-19 消费 298 元，服务套餐为“欧莱雅烫染护理”，当前余额 702 元。
- 只新增 8 条已完成套餐核验记录，张三、李四、王五、赵六各 2 条；原库已有 1 条，所以数据库总数显示为 9 条。
- 写入后运行留存扫描，当前生成 114 条提醒：复购 50 条、生日 16 条、流失风险 48 条；页面展示时会按规则重新分组，所以当天页面总数会随数据变化。

#### 为什么这样改

员工助手和留存 Agent 只有连接真实业务数据，才能演示“查询结果来自数据库”和“留存规则基于历史到店节奏”。这次没有让每个人拥有相同次数或完全随机的金额，而是把客户集中在 2—4 次到店、近 3 个月到店，同时保留少量低频客户和长期未到店客户，更接近真实门店数据，也方便观察不同客户被如何分层。

当前 `.env` 没有填写 `DATABASE_URL`，所以本次数据实际写入项目默认的 `hair_salon.db` SQLite。种子脚本使用 SQLAlchemy ORM，字段和 PostgreSQL 兼容；以后切换 PostgreSQL 时，只需先配置 `DATABASE_URL`，再运行同一条命令。

#### 如何验证

- 运行 `python -m backend.database.seed_demo_customers` 后检查上述到店次数、消费金额和最近到店区间；再次运行应得到同一批演示数据，不会出现手机号重复。
- 在员工工作台的“客户与会员”中搜索“龙百川”或手机号 13370000003，检查余额 702 元、消费 298 元和套餐名称。
- 在“员工管理”中查看四位发型师的服务记录；套餐核验记录本次每人 2 条。
- 在“留存提醒”中运行运营分析，观察近期客户、余额客户和长期未到店客户的分层结果。

#### 面试怎么讲

> 我没有直接往数据库里随机塞字符串，而是通过 SQLAlchemy 按真实业务关系生成数据：用户关联会员和钱包，消费关联历史预约，套餐核验关联套餐、客户、预约和发型师。生成器使用固定随机种子和分层分布，让数据既有可重复性，又不像平均分配的假数据；这样留存 Agent 的结果可以追溯到真实字段，而不是写死的演示文案。

#### 下一步

如果要在 PostgreSQL 环境演示，先把 `DATABASE_URL` 配成 PostgreSQL 连接串并初始化表，再执行 `python -m backend.database.seed_demo_customers`；当前 `.env` 未配置该变量，所以本地实际使用的是 `hair_salon.db` SQLite。

### 第 44 步：拆分留存提醒模块并补充可解释条件（2026-07-14）

#### 本步改了什么

- 在 `frontend/staff.js` 中把留存提醒拆成“流失风险、生日提醒、复购提醒”三个独立模块，并显示每个模块的数量。
- 增加“分析判断条件”面板，明确数据来源以及流失、生日、复购和余额客户的判断规则；提醒卡片还会显示对应分析证据。
- 在 `frontend/staff.css` 中给每个提醒模块增加独立滚动窗口，避免 119 条提醒把页面无限拉长。
- 在 `frontend/staff.html` 中更新静态资源版本号，确保浏览器加载本次前端改动，而不是继续使用旧缓存。

#### 为什么这样改

之前所有提醒混在一个长列表里，员工很难快速区分“马上挽回流失客户”和“提前做生日触达”。把任务按业务动作分块后，员工可以先看高风险客户，再处理生日和复购；把规则直接展示出来，则能回答“为什么推荐这个客户”，提高 Agent 结果的可解释性。

#### 如何验证

- 在员工工作台打开“留存提醒”，确认页面有 3 个模块：流失风险 50 条、生日提醒 17 条、复购提醒 52 条。
- 检查每个模块的滚动区域高度为 420px 左右，内容高度大于容器高度，并且 `overflow-y` 为 `auto`。
- 检查“分析判断条件”面板显示数据来源和四条判断规则。
- 运行 `node --check frontend/staff.js`、相关 Python 编译检查和 `git diff --check`。

#### 面试怎么讲

> 留存页不是只展示模型结论，而是把提醒按运营动作分组，并把判断依据一起展示。页面层负责分组和交互，后端规则负责计算，Agent 只能基于客户真实到店、生日和余额字段生成待办；这样员工既能快速处理，也能解释每条提醒为什么出现。

#### 下一步

继续用员工助手测试组合查询和多轮追问；如果要体现真正的自然语言能力，需要给 `.env` 配置有效的模型服务余额或替换可用的 API 配置。

### 第 45 步：修正预约演示数据和员工调整表单（2026-07-14）

#### 本步改了什么

- 在 `backend/database/seed_demo_customers.py` 中为四位发型师各生成 3 条近期“已确认”预约，并自动补齐未来 7 天的可用时间段；重复运行仍会先清理演示客户预约再重建。
- 在 `frontend/staff.js` 中把预约调整表单改成下拉选择：员工选择客户姓名、手机号、预约时间和发型师，不再手动粘贴预约 UUID、时间槽 UUID 或发型师 UUID。
- UUID 仍由后台作为真实关联键使用，前端只展示业务人员看得懂的姓名、手机号、日期和时间。
- 修正前端“今天”的日期计算，使用本地时区，避免中国时区下把当天预约查成前一天。

#### 为什么这样改

预约 UUID 是数据库内部主键，不适合让员工手工填写；时间槽 UUID 代表“某位发型师在某天某时段”的记录，也不是员工需要理解的业务概念。现在页面负责展示人话，后台仍用 UUID 做精确校验，既降低操作门槛，又保留防止重复预约的安全边界。

#### 如何验证

- 执行 `python -m backend.database.seed_demo_customers`，结果新增 12 条近期预约。
- 查询数据库确认张三、李四、王五、赵六各有 3 条今日已确认预约。
- 浏览器验证日期为 `2026-07-14`，四位发型师各显示 3 条预约；调整表单显示 12 条预约选项和 168 个时段选项，标签中没有 `ID`。
- 运行 `node --check frontend/staff.js`、Python 编译检查和 `git diff --check`。

#### 面试怎么讲

> 我把业务展示层和数据关联层分开：员工通过姓名、手机号、日期和时间选择预约，后台再使用 UUID 做唯一定位和并发校验。这样既符合业务人员的使用习惯，也没有牺牲数据库关系和预约安全性；同时统一使用本地日期，避免时区导致查询错一天。

#### 下一步

继续验证预约调整的“生成方案—人工确认—写入数据库”完整链路，并补充一条前端操作演示记录。

### 第 46 步：提醒卡片改为点击后展开客户列表（2026-07-14）
#### 本步改了什么

- 留存提醒页默认只显示“分析判断条件”和操作提示，不再一打开就把流失、生日、复购客户全部铺在页面上。
- “流失风险”“生日提醒”“复购提醒”变成可点击的入口卡片；点击后只展开对应模块，并自动滚动到该模块。
- 每个客户列表仍然保留独立滚动窗口，避免一次显示大量客户导致页面过长；“余额客户”继续作为规则说明展示，因为当前提醒接口没有单独的余额客户列表。
- 给入口卡片增加悬停、按下和键盘聚焦状态，并更新 `frontend/staff.html` 的静态资源版本，确保浏览器加载最新脚本和样式。

#### 为什么这样改

之前规则说明和客户列表同时展示，员工进入页面后会被大量数据淹没，找不到先处理哪一类任务。现在采用“先看分类、再点开列表”的渐进式展示：入口负责导航，模块负责处理客户，既减少首屏信息量，也保留原来的真实数据和操作能力。

#### 如何验证

- 刷新员工工作台并进入“留存提醒”，确认初始状态看不到三个客户列表，只看到点击提示。
- 点击“流失风险”“生日提醒”“复购提醒”，确认每次只显示对应列表，并自动滚动到对应模块。
- 确认列表内部仍可以滚动，运行 `node --check frontend/staff.js` 和 `git diff --check`。

#### 面试怎么讲

> 我把留存页面设计成渐进式披露：先展示规则和待办分类，员工点击某个运营动作后才展开对应客户列表。这样减少首屏认知负担，同时通过按钮的数量、聚焦态和滚动定位保证入口清晰；后端数据接口和留存规则不变，前端只负责展示分组和交互。

#### 下一步

在浏览器中完成点击、滚动和键盘聚焦验证；如果交互符合预期，再继续优化其他页面的入口层级。

### 第 47 步：补齐生日与复购扫描客户（2026-07-14）
#### 本步改了什么

- 修复运营分析只声明“流失风险、余额客户”的问题，Agent 现在同时识别生日提醒和复购提醒。
- Agent 改为复用留存引擎的统一客户判定规则，生日窗口、个人复购周期和流失阈值不再维护两套逻辑。
- 前端把本次 Agent 扫描出的生日/复购客户合并到对应的隐藏模块；点击“生日提醒”后可以看到真实姓名、手机号、生日依据和常用发型师。
- 保留已有待办优先展示并去重；Agent 新发现的客户作为只读扫描结果展示，不会自动写入提醒、发送消息或修改数据库。
- 更新员工工作台静态资源版本，避免浏览器继续使用旧版缓存。

#### 为什么这样改

之前数据库留存引擎已经能生成生日和复购提醒，但 Agent 只复制了流失和余额两条规则，导致页面扫描结果与实际留存规则不一致。现在让 Agent 调用同一个客户判定入口，再由前端把扫描结果放回对应模块，保证“规则判断—扫描结果—页面展示”是一条链路。

#### 如何验证

- 运行 `pytest -q tests/test_retention_agent.py`，确认 Agent 规则目录包含流失、生日、复购和余额，并能返回生日/复购客户。
- 在员工工作台重新运行运营分析，确认“生日提醒”和“复购提醒”入口出现。
- 点击“生日提醒”，确认列表显示扫描客户的生日依据；点击“复购提醒”，确认显示个人节奏和到店天数。
- 运行 `node --check frontend/staff.js`、Python 编译检查和 `git diff --check`。

#### 面试怎么讲

> 留存 Agent 不再复制一套独立规则，而是调用留存引擎统一的客户判定入口。这样生日、复购和流失的判断条件只有一个来源；Agent 返回可解释的证据，前端再按运营动作分组展示，扫描结果不会变成孤立的统计数字。

#### 下一步

在浏览器中重新运行一次运营分析，重点确认生日提醒客户能展开、证据文案正确，并检查扫描结果与待跟进列表的去重效果。

### 第 48 步：补充已有提醒的分析依据展示（2026-07-14）
#### 本步改了什么

- 在 `ReminderResponse` 中增加可选的 `evidence` 字段，让数据库里已经存在的提醒也能返回可解释依据。
- 后端根据提醒类型即时生成依据：生日显示生日日期和剩余天数；复购显示距上次到店天数、个人周期和提醒阈值；流失显示距上次到店天数和风险阈值。
- 增加接口测试，验证旧的生日提醒记录经过员工端接口后仍能返回依据。

#### 为什么这样改

之前只有本次 Agent 新扫描出来的记录带有证据，历史数据库提醒虽然能正常展示客户，却没有说明“为什么提醒”。把依据放到接口响应中，前端不用区分新旧记录，就能统一展示，也避免为了补显示字段而修改历史数据库结构。

#### 如何验证

- 运行 `pytest -q tests/test_retention_agent.py`，确认生日提醒接口返回生日日期和剩余天数。
- 运行 `node --check frontend/staff.js`、`python -m compileall -q backend` 和 `git diff --check`。
- 测试接口已确认旧生日提醒返回独立 `evidence` 字段；浏览器重启后受到本地地址安全策略限制，暂未完成卡片的二次点击复验。

#### 面试怎么讲

> 数据库里旧提醒没有单独保存证据 JSON，所以我没有强行改表或回填历史数据，而是在查询响应层基于客户生日、提醒触发日期和留存规则重新生成可解释文案。这样兼容旧数据，同时保证前端对新旧提醒使用同一套展示协议。

#### 下一步

重新登录员工工作台并点击“生日提醒”，确认生日客户卡片能看到依据文案；如果本地服务已重启，应使用当前服务对应的员工账号登录。

### 第 49 步：形成完整留存提醒优化方案（2026-07-14）

#### 本步改了什么

- 新增 `docs/留存提醒完整优化方案.md`，把生日提醒、复购提醒和流失风险统一到同一个留存工作台方案中。
- 文档补充统一拦截规则、跨类型冷却、同客户任务合并、Agent 职责边界、页面结构、数据表、接口、状态流转、异常处理和验证用例。
- 明确“余额客户”是复购或流失任务的策略标签，不单独生成第四类提醒，避免同一客户产生重复任务。
- 本步只形成评审文档，没有修改现有后端、数据库、Agent 或前端代码。

#### 为什么这样改

之前的优化重点偏向长期流失召回，没有把生日、复购和流失放在一套完整的防打扰机制中。实际业务中同一客户可能同时生日、长期未到店并且还有余额，如果三类规则各自生成任务，就可能连续联系客户。新文档先固定统一业务口径，为后续分步骤开发提供依据。

#### 和哪些模块有关

- `backend/database/retention.py`：后续需要统一三类规则、跨类型冷却和冲突合并。
- `backend/database/models.py` 与数据库迁移：后续需要补任务、联系和忽略数据。
- `backend/agents/retention_graph.py`：后续只分析已经通过规则筛选的客户。
- `backend/api/routers.py`：后续增加发送、重试、人工跟进和忽略接口。
- `frontend/staff.js`、`frontend/staff.css`：后续改造成今日待处理和联系记录工作台。

#### 如何验证

- 打开 `docs/留存提醒完整优化方案.md`，检查是否同时覆盖生日、复购和流失风险。
- 检查文档是否明确 150 天边界、三类冷却期、退订优先级和同客户单任务原则。
- 检查流程图、状态图、接口、数据字段和测试清单是否能够对应后续开发步骤。

#### 面试怎么讲

> 我没有直接在原来的三个列表上继续堆按钮，而是先把留存提醒整理成统一业务模型。生日、复购和流失共享退订、忽略、人工跟进和冷却规则；同一客户同一天只生成一个任务，余额只影响策略而不单独生成任务。这样规则可以统一测试，Agent 只负责个性化建议，页面负责员工审核和操作。

#### 下一步

等待确认完整方案和默认参数。确认后从第一阶段开始，只修改规则引擎和对应测试；完成并讲解验证结果后，再单独申请下一阶段修改。

### 第 50 步：建立留存工作台数据基础（2026-07-14）

#### 本步改了什么

- 新增 `RetentionTask`、`RetentionContact`、`RetentionSuppression` 三个数据库模型。
- `RetentionTask` 保存当天唯一的留存任务、主提醒类型、合并后的触发原因、证据快照、建议内容和下次允许联系时间。
- `RetentionContact` 保存实际发送内容、审核/发送员工、渠道、成功或失败时间、失败原因和客户回复。
- `RetentionSuppression` 保存限时忽略、永久忽略、退订和人工跟进，避免用单个“已忽略”状态丢失期限与原因。
- 新增 Alembic 迁移 `0010_add_retention_workbench.py`，并保留旧 `ReminderLog` 作为历史兼容数据，不再把它当作新工作台的权威来源。

#### 为什么这样改

旧提醒表只能表达“待处理、已联系、已忽略”，不能回答谁审核、实际发了什么、发送是否失败、什么时候可再次联系。把任务、实际联系和拦截状态拆开后，规则引擎、Agent、接口和页面都使用同一份真实记录，后续不会出现两套状态互相矛盾。

#### 如何验证

- `python -m compileall -q backend migrations` 通过。
- 在内存 SQLite 中导入模型后执行 `init_db()`，确认创建 `retention_tasks`、`retention_contacts`、`retention_suppressions` 三张表。
- 使用独立 SQLite 文件运行 `python -m alembic upgrade head`，确认从初始迁移完整升级到 `0010_add_retention_workbench`。
- `git diff --check` 通过。

#### 面试怎么讲

> 我把“提醒任务”和“实际触达”分开建模。任务回答为什么今天需要处理，联系记录回答员工最后发了什么、结果如何，抑制记录回答为什么系统暂时不能再联系。这样冷却期、忽略和发送失败都有可追溯依据，而不是依赖一个真假字段。

#### 下一步

重构留存规则引擎：以新任务表作为扫描结果，先拦截退订、忽略、人工跟进和冷却客户，再合并生日、复购、流失候选，确保同一客户同一天只有一个任务。

### 第 51 步：重构统一留存规则引擎（2026-07-14）

#### 本步改了什么

- `backend/database/retention.py` 改为使用新留存任务表扫描客户，不再为新工作台写入旧 `ReminderLog`。
- 固定流失边界为“距上次到店大于等于 150 天”；个人复购周期只用于复购阈值和解释依据。
- 将生日与生命周期候选合并：同一客户当天只保留一条主任务，生日优先；余额作为 `balance_customer` 策略标签，不单独生成任务。
- 在业务规则前增加统一拦截：有效忽略、退订、人工跟进和成功发送后的全局冷却期都会阻止新任务。
- 流失、复购、生日使用不同冷却期；旧 `ReminderLog` 的已联系记录在迁移期间仍提供 14 天保护。
- 移除“备了一份生日护理”和“店里有回归优惠”等未经配置的营销承诺。
- 新增 `tests/test_retention_workbench.py`，覆盖关键边界和幂等扫描。

#### 为什么这样改

留存系统最重要的是先判断“今天能不能联系”，再讨论“怎么联系”。现在生日、复购和流失都经过同一套拦截和合并逻辑，员工不会因为同一个客户同时有生日和余额而看到两条任务。固定 150 天流失线也保证测试和面试口径一致，个人周期仍保留在复购判断中，避免所有客户都按同一天数提醒。

#### 如何验证

- `python -m pytest tests/test_retention_workbench.py -q`：5 passed。
- 用例验证 149 天客户仍可能是复购提醒，但不会被判为流失风险；150 天客户进入流失风险。
- 用例验证生日加余额流失合并为一条生日主任务，且保留余额标签和两个触发原因。
- 用例验证忽略、成功发送后的冷却和重复扫描都不会创建额外任务。
- `python -m compileall -q backend tests` 与 `git diff --check` 通过。

#### 面试怎么讲

> 我把留存判断做成先拦截、再命中、再合并的三步规则。退订、忽略和冷却期先挡住不该联系的人；生日、复购和流失再分别计算；最后同一客户当天只保留一个主任务。余额不是第四种任务，而是影响流失优先级和话术方向的标签，所以不会产生重复触达。

#### 下一步

新增留存工作台接口：查询今日任务与历史任务，员工确认发送、重试、人工跟进、设置忽略、模拟客户回复和关闭任务都会写入新的联系或抑制记录，并保留审计日志。

### 第 52 步：实现留存工作台接口与模拟发送（2026-07-14）

#### 本步改了什么

- 新增 `/api/retention/tasks` 任务查询、详情、发送、重试、人工跟进、忽略、回复和关闭接口。
- 新增 `MockMessageSender`，模拟渠道发送会返回可追踪的消息编号；预留 `WeChatMessageSender` 接口，后续接微信时不改规则层。
- 发送前后端重新检查客户是否退订、忽略、人工跟进或处于冷却期，防止前端列表过期后仍然发送。
- 发送成功后保存实际消息、审核人、发送人、渠道编号、客户通知、审计记录和 `next_contact_at`；发送失败只保存失败记录，不进入冷却。
- 忽略支持 30 天、90 天、永久和退订；客户回复自动创建人工跟进拦截，关闭任务会解除人工跟进拦截。
- 当前没有优惠券主数据表，发送接口拒绝任意优惠券编号，防止 Agent 或前端虚构权益。

#### 为什么这样改

员工点击按钮不是简单改一个状态，而是一次有业务含义的操作。例如发送失败不能算已联系，客户回复后不能继续自动营销，人工完成跟进后又应该允许未来重新扫描。把这些规则放在后端接口中，页面只负责发起操作，才能避免不同页面或未来小程序绕过限制。

#### 如何验证

- `python -m pytest tests/test_retention_workbench.py -q`：8 passed。
- 用例验证发送成功会创建完整联系记录和模拟渠道编号，重复发送返回 409。
- 用例验证模拟失败保留 `send_failed` 状态且可通过 `/retry` 成功重试。
- 用例验证忽略、回复和关闭任务会正确创建或解除抑制记录。
- `python -m compileall -q backend tests migrations` 与 `git diff --check` 通过。

#### 面试怎么讲

> 我把发送动作设计成服务端状态机，而不是前端点一下就改成已联系。服务端会先重新检查客户资格，再创建一次联系尝试；成功才写冷却，失败保留重试入口。这样即使多个员工同时操作，或者页面数据已经过期，也不会绕过退订和频率控制。

#### 下一步

重构留存 Agent：它不再重新扫描并单独生成余额客户，而是读取规则引擎已创建的任务，输出包含策略、话术、优惠券空值、推荐理由和风险提示的结构化建议；模型不可用时仍使用安全模板。

### 第 53 步：收紧留存 Agent 的输入输出边界（2026-07-14）

#### 本步改了什么

- `backend/agents/retention_graph.py` 不再独立扫描后重复追加“余额客户”候选，而是先触发幂等扫描，再读取今日留存任务。
- Agent 每条建议统一返回任务 ID、主类型、合并触发原因、策略标签、策略名称、可编辑话术、优惠券空值、推荐原因和风险提示。
- 当前使用 `safe_template` 安全模板模式：没有模型密钥也能生成稳定建议，不会因模型服务不可用阻塞工作台。
- 余额客户会显示“只能陈述真实余额，不得制造余额失效紧迫感”的风险提示；多个触发原因会提示已合并，避免重复触达。
- 更新 Agent 测试，验证有余额的流失客户只返回一条流失建议，并保留余额标签而非独立余额任务。

#### 为什么这样改

Agent 的职责是解释和建议，不是再维护一套客户筛选规则。让它直接读取规则任务后，流失线、冷却和忽略只有一个数据来源；即使以后接大模型，也只能替换建议生成节点，不能绕过已验证的业务资格判断。

#### 如何验证

- `python -m pytest tests/test_retention_agent.py tests/test_retention_workbench.py -q`：14 passed。
- 验证 Agent 对余额流失客户只返回一条 `churn_risk` 建议，包含 `balance_customer` 标签、空优惠券和安全模式标记。
- 验证生日和复购客户仍出现在建议中，Agent 执行不创建客户通知。
- `python -m compileall -q backend tests` 与 `git diff --check` 通过。

#### 面试怎么讲

> 留存 Agent 不直接决定谁该触达，它读取规则引擎已经生成的任务，再把规则证据整理成策略、话术和风险提示。当前先用安全模板保证没有模型服务也能稳定运行；以后替换成大模型时，输入输出 schema 和后端校验不变，因此模型不能绕过退订、冷却和优惠券限制。

#### 下一步

重构员工端留存提醒页面：默认显示今日待处理任务，支持生日/复购/流失筛选、联系记录视图、详情抽屉、发送重试、人工跟进、忽略和客户回复操作。

### 第 54 步：重构员工端留存工作台（2026-07-14）

#### 本步改了什么

- 员工端留存页面改为“今日待处理”和“联系记录”两个主视图，默认直接显示今天允许员工处理的任务。
- 顶部显示总任务、生日、复购、流失风险和发送失败数量；支持全部、生日、复购、流失四种筛选及姓名/手机号搜索。
- 表格展示客户、类型、合并后的触发原因、最近联系、下次可联系、Agent 建议和当前状态。
- 点击“查看”打开详情抽屉，员工可编辑消息并确认发送或重试，也可以转人工、设置 30/90 天或永久忽略、记录客户回复和完成跟进。
- 更新 `staff.html` 静态资源版本，避免浏览器继续加载旧的留存页面脚本和样式。
- 使用本地演示库中的张三账号完成真实浏览器验证，并完成一条模拟发送，任务从今日待处理自动进入联系记录与冷却状态。

#### 为什么这样改

旧页面要求员工先点分类卡片才看到客户，而且只能处理旧提醒的“已联系/忽略”两种状态。新页面以当天工作为第一入口，把历史、冷却和忽略放到记录视图，员工能先处理最需要做的事；详情抽屉把消息编辑、原因、联系历史和人工操作放在同一个上下文里，不需要跳转页面。

#### 如何验证

- `node --check frontend/staff.js` 通过。
- `python -m pytest tests/test_staff_web.py tests/test_retention_agent.py tests/test_retention_workbench.py -q`：15 passed。
- 本地开发库已升级到 Alembic `0010_add_retention_workbench`。
- 在 `http://127.0.0.1:8001/staff` 使用张三账号登录，运行今日扫描后显示 117 条任务：生日 16、复购 88、流失风险 13。
- 在浏览器打开顾明远的生日任务并确认模拟发送后，今日待处理减少为 116；联系记录显示“今天已联系、已发送”和下次可联系日期。
- 已生成桌面和手机宽度截图：`output/playwright/retention-desktop.png`、`output/playwright/retention-mobile.png`。

#### 面试怎么讲

> 留存页面按员工当天的工作顺序设计：默认只显示当前可处理任务，生日、复购和流失用筛选而不是三个分散名单。员工打开任务后可以看到规则证据、Agent 建议和完整联系历史，再决定发送、转人工或忽略。所有动作调用同一套后端状态接口，所以页面展示和频率控制始终一致。

#### 下一步

执行全量测试、迁移检查和最终接口回归，检查文档与实际实现是否一致；完成后整理最终演示入口和验证结果。

### 第 55 步：完成留存工作台回归与交付验证（2026-07-14）

#### 本步改了什么

- 执行完整测试集并修复员工查询 Agent 在模型路径和规则路径下返回协议不一致的问题。
- 员工查询接口现在统一使用受控 Graph 校验动作、意图、数据来源和 trace；模型仍负责组织回答，但不能丢失工具调用证据。
- 查询预约时，如果模型遗漏真实预约 ID，接口会补回已校验的日程结果；护理知识回复会附带实际检索到的知识库标题。
- 本地开发数据库已升级到 `0010_add_retention_workbench`，并重启最新代码服务到 `http://127.0.0.1:8001`。

#### 为什么这样改

同一个员工问题在“模型可用”和“模型不可用”时，不能得到两种无法追踪的接口结构。现在无论模型是否参与，员工都能看到查询使用了什么工具、数据来自哪里；模型只负责语言表达，真实数据和可操作 ID 始终由受控查询提供。

#### 如何验证

- `python -m pytest -q`：87 passed，1 条 TestClient 弃用警告。
- `python -m alembic current`：`0010_add_retention_workbench (head)`。
- `node --check frontend/staff.js`、`python -m compileall -q backend migrations tests` 和 `git diff --check` 通过。
- 浏览器端已完成扫描、打开任务、模拟发送和联系记录核验；桌面与手机截图均已保存到 `output/playwright/`。
- 本地服务健康检查：`http://127.0.0.1:8001/api/health` 返回正常。

#### 面试怎么讲

> 我不仅测试了新增留存规则，还跑了全项目回归。测试中发现模型路径和规则降级路径的动作标识不一致，于是把员工 Agent 的对外协议统一为受控 Graph 的工具、来源和 trace。这样模型负责自然语言，真实数据与审计信息由确定性流程负责，既保留智能交互，也保证结果可解释和可复现。

#### 下一步

留存工作台第一版已完成。演示时先登录员工端，进入留存提醒并运行今日扫描；打开任一任务可以展示规则证据、可编辑话术、模拟发送、冷却记录和人工跟进流程。

### 第 56 步：统一长数据详情为右侧滑动窗口（2026-07-14）

#### 本步改了什么

- 保留留存任务原有的右侧详情抽屉，并补上从右侧滑入的过渡动画。
- 操作审计页面改为“列表摘要 + 查看详情”：主表只显示时间、动作和业务对象；点击“查看详情”后，在右侧抽屉查看操作人、审计编号和完整记录详情。
- 客户账户流水不再追加到客户列表底部，改为在右侧抽屉中查看余额、累计充值和完整流水。
- 三种抽屉共用同一套宽度、滚动和关闭行为；可以点击关闭按钮或按 `Esc` 关闭。手机端抽屉占满屏幕，宽表格只在抽屉内部横向滚动。
- 更新员工端静态资源版本号，避免浏览器缓存旧脚本和样式。

#### 为什么这样改

审计记录和账户流水都是“先扫一眼、需要时再深入看”的数据。若直接把完整信息铺在主页面，员工在找下一条任务时会被大量详情打断；放到右侧抽屉后，主列表不丢失当前位置，详情又能在同一个页面内快速打开和关闭。

#### 如何验证

- `node --check frontend/staff.js` 和 `git diff --check` 通过。
- 在 `http://127.0.0.1:8001/staff` 登录员工账号，打开“操作审计”，点击任一“查看详情”，确认抽屉显示动作、对象、操作人、审计编号和完整 JSON 详情；按 `Esc` 后抽屉关闭。
- 打开“客户与会员”，点击余额客户的“查看流水”，确认流水在抽屉中显示，未重新拉长客户主表。
- 使用 Playwright 在桌面和 `375 x 812` 手机宽度验证。手机端抽屉铺满屏幕，流水表格横向滚动限制在抽屉内部。

#### 面试怎么讲

> 对于审计、账户流水和留存联系历史这类长数据，我采用“主列表负责定位，侧边抽屉负责追溯”的交互。员工处理任务时保留原来的筛选和滚动位置，点击后才加载或展开详细证据。这样既保证操作效率，也保留了审计所需要的完整可追溯性；在手机端抽屉自动全屏，避免宽表格破坏页面布局。

#### 下一步

后续新增任何“列表点进去看大量历史明细”的功能时，优先复用这一套抽屉组件和关闭规则，而不是把详情直接插入主表格下方。

### 第 57 步：修正审计列表为固定区域纵向滚动（2026-07-14）

#### 本步改了什么

- 为操作审计表增加独立的固定高度滚动区域，超出高度时只在表格内部上下滚动。
- 审计表头在纵向滚动时固定在顶部，便于持续识别时间、动作、业务对象和操作列。
- 审计摘要表改为固定列宽和长文本自动换行，移除主表横向滚动条；完整编号和 JSON 详情仍通过“查看详情”进入右侧抽屉。
- 审计滚动区域可获得键盘焦点，因此也可以用键盘滚动查看历史记录。

#### 为什么这样改

审计记录本质上是一条按时间向下追溯的日志流。横向滚动会遮住“查看详情”按钮，也会打断员工按时间浏览记录的动作；固定高度加纵向滚动更符合这个业务数据的阅读方向。

#### 如何验证

- `node --check frontend/staff.js` 与 `git diff --check` 通过。
- 在浏览器打开“操作审计”，确认列表区域右侧出现内部纵向滚动条，页面底部不再出现审计表的横向滚动条。
- 聚焦审计列表并按 `End` 滚动到底部，确认表头仍固定在该列表顶部。
- 验证截图：`output/playwright/audit-vertical-scroll.png`、`output/playwright/audit-vertical-scroll-bottom.png`。

#### 面试怎么讲

> 审计日志是按时间追溯的数据，主交互应该是纵向浏览。所以我把列表限制在固定高度内，让表头吸顶，长编号在摘要层自动折行，完整内容进入侧边详情。这样既避免横向拖动，也保留了完整的可追溯信息。

#### 下一步

其他列表型页面在数据量增长后，也按“业务阅读方向”选择滚动方式：时间流优先纵向滚动，只有确实无法压缩的财务明细才在局部区域提供横向滚动。
### 第 58 步：只把真实触达放入联系记录（2026-07-14）

#### 本步改了什么

- 联系记录接口不再把所有“不是今日待处理”的任务都返回。
- 只有存在发送尝试记录，或已经进入发送、发送失败、客户回复、人工跟进、冷却、完成等真实处理链路的任务，才会进入“联系记录”。
- 纯扫描生成的 pending_review 任务，以及没有任何联系事实的忽略任务，不会出现在联系记录。
- 页面说明同步改为“只有已有发送尝试或人工跟进事实的客户才显示”。
- 增加回归测试，覆盖未联系、人工跟进和发送失败三类任务。

#### 为什么这样改

扫描任务只是系统根据生日、复购或流失规则算出的“可能需要处理”，不等于员工已经联系客户。如果把扫描结果直接叫作联系记录，员工会误以为系统已经触达客户，审计和数据分析也会失真。联系记录必须来自实际发送尝试或人工介入事实。

#### 如何验证

- RetentionService.list_tasks(records_only=True) 会排除没有联系事实的 pending_review。
- 人工跟进任务和带有失败联系记录的任务会保留在联系记录，便于员工继续处理和重试。
- 前端联系记录说明与后端筛选口径一致。

#### 面试怎么讲

> 我把“待处理任务”和“联系记录”分成两个事实层。规则扫描只产生待处理任务，只有发送接口创建了 RetentionContact，或者员工明确转人工，才算进入联系记录。这样系统不会把“准备联系”误报成“已经联系”，失败发送也能留下尝试记录并支持重试。

#### 下一步

用真实浏览器重新登录员工端，验证今日待处理仍显示未联系任务，切换联系记录后只显示发送或人工跟进过的客户；再执行全量测试和页面回归。
### 第 59 步：修正联系记录页的视图计数（2026-07-14）

#### 本步改了什么

- 联系记录页的“全部、生日、复购、流失”筛选数量改为当前联系记录集合的数量。
- 顶部的“今日待处理、生日、复购、流失风险、发送失败”仍保留为当天工作总览，不与历史记录数量混用。
- 浏览器核验中，当前示例数据显示联系记录 3 条，其中生日 1 条、复购 2 条、流失 0 条，与表格内容一致。

#### 为什么这样改

同一页面同时有“今日工作指标”和“历史联系记录”。如果筛选按钮仍显示今日任务总数，员工会看到“联系记录 3 条”但筛选却显示“全部 114”，容易误以为数据加载错误。两个数字来源分开后，指标负责总览，筛选负责当前列表。

#### 如何验证

- 浏览器打开员工端留存提醒，今日待处理显示 114 条。
- 切换联系记录后，表格显示 3 条，筛选显示“全部 3、生日 1、复购 2、流失 0”。
- 联系记录中的客户均有已发送事实，没有纯待审核任务。

#### 面试怎么讲

> 我把页面中的统计口径拆开：顶部是今天要做多少工作，列表筛选是当前视图有多少条记录。这样运营人员不会把待处理数量和历史触达数量混为一谈，数据分析也更可靠。

#### 下一步

继续按真实门店上线标准做预约核验、会员等级、账户流水和长页面滚动的最终浏览器回归。
### 第 60 步：重启后完成最终回归（2026-07-14）

#### 本步改了什么

- 电脑重启后重新检查工作区、API 服务和浏览器页面，确认前面两项修改没有因为中断丢失。
- 重新运行全项目测试，结果为 88 passed，1 条依赖弃用警告。
- 员工端健康检查返回正常。
- 浏览器重新核对今日待处理与联系记录：今日待处理 114 条，联系记录 3 条；联系记录筛选为全部 3、生日 1、复购 2、流失 0。
- 重新核对客户与会员：王复购显示 VIP1、李生日显示 VIP2、张流失显示 VIP4；账户流水通过右侧抽屉查看。
- 核验专项测试确认日程状态为已核验，员工概览有核验记录，完成服务前不计入订单和绩效，完成后才进入绩效。

#### 为什么这样改

重启会停止本地服务，也可能中断测试进程，所以不能只依赖重启前的口头状态。重新从文件、接口、测试和浏览器四个层面核对，才能确认代码已保存、服务可启动、页面实际加载的是最新逻辑。

#### 如何验证

- python -m pytest -q：88 passed。
- python -m compileall -q backend tests migrations、node --check frontend/staff.js、git diff --check 均通过。
- http://127.0.0.1:8001/api/health 返回 status ok。
- Playwright 页面快照确认今日待处理、联系记录、会员等级和流水抽屉均正常。

#### 面试怎么讲

> 我把回归分成代码、接口、数据和页面四层。重启后先确认服务健康，再跑全量测试，最后用真实员工账号检查关键页面，避免出现“代码通过但页面还是旧缓存”或“页面能看但后台状态不一致”的问题。

#### 下一步

当前两项用户反馈和本轮上线前审核已完成。后续接入真实微信渠道前，继续保留模拟发送和完整审计记录，等渠道、优惠券库存和自动发送策略经过单独验收后再开放。

### 第 61 步：员工查询与店长操作权限分层（2026-07-15）

#### 本步改了什么

- 确认手机号 `19158260000` 的现有账号已启用并保留为 `ADMIN`，作为店长账号；没有重置它原来的密码。
- 普通员工仍能查询日程、客户、会员和护理知识，但不能操作预约、服务核验、退款、套餐或留存任务。
- 店长才可以代客户预约、调整预约、核验完成服务、审批退款、操作留存任务、查看钱包流水、营业概览和操作审计。
- 员工登录后，前端不再请求退款、审计、钱包和营业额数据，也不会显示写操作按钮；后端接口仍会再次校验角色，防止绕过页面直接请求。
- 客户和会员列表对普通员工不再返回累计消费和账户余额，避免把“简单查询”变成财务数据浏览。

#### 为什么这样改

页面上不显示按钮不等于安全，别人仍然可以直接调用接口。权限必须同时在前端和后端控制：前端负责让界面清楚，后端负责真正拦截。这样 Agent 后面即使识别到了“批复预约”，普通员工的登录令牌也不能让它写入数据库。

#### 和哪些模块有关

- `backend/api/routers.py`：工作台写接口和敏感读取接口改为店长权限。
- `backend/api/schemas.py`：员工读取客户、会员信息时不再包含金额字段。
- `frontend/staff.js`：按登录角色加载数据、展示导航和控制按钮。
- `tests/`：原先假定发型师可以写入的测试改为店长执行，并新增普通员工被拒绝的验证。

#### 如何验证

- `node --check frontend/staff.js`、`python -m compileall -q backend`、`git diff --check` 通过。
- 权限相关自动化测试共 `32 passed`。
- 测试确认普通员工能读取客户和日程，但请求 `/api/staff/appointments` 返回 403；店长账号能成功代约，预约会同步到客户预约列表和员工日程。

#### 面试怎么讲

> 我把员工工作台拆成查询权限和管理权限。前端按角色隐藏敏感数据和操作入口，后端再根据 JWT 中真实的角色做最终校验。这样不是“看不到按钮就算安全”，而是即使绕过页面请求接口，普通员工也无法执行写操作。店长账号才有受控操作能力，后续 Agent 调用的也是同一套后端权限。

#### 下一步

让客户预约先进入待批复状态，再由店长 Agent 生成操作预览、确认后把预约改为已确认，并写入审计和客户通知。

### 第 62 步：预约待批复与确认闭环（2026-07-15）

#### 本步改了什么

- 顾客从 H5、聊天或小程序共用预约接口提交预约时，状态从“直接已确认”改为 `pending`（待店长确认）。
- 店长在员工工作台代客户创建预约时，仍然可以直接创建为 `confirmed`，因为这是店长已经明确执行的操作。
- 增加“预约批复任务”：店长先查看客户、项目、时间和发型师，再点击确认；确认时才把预约从 `pending` 改为 `confirmed`。
- 批复使用条件更新：只有数据库里仍是 `pending` 的预约才会成功，重复点击或别人已经处理的预约会被拒绝。
- 批复成功后，在同一事务内创建客户通知和审计记录；员工日程、客户 H5 和小程序读取的是同一条预约，因此状态会同步变化。
- 工作台日程为店长显示“批复预约”按钮；普通员工只能查看待确认状态。

#### 为什么这样改

以前虽然数据模型有 `pending`，但创建预约时直接写成 `confirmed`，所以“批复”只是口头概念。现在先有待办状态，再经过店长确认，才能真正体现门店审核流程。确认按钮也避免了 Agent 只凭一句话就直接改订单。

#### 和哪些模块有关

- `backend/database/service.py`：预约创建支持传入状态。
- `backend/client/appointment.py`、`backend/agents/chat_agent.py`：顾客侧预约提示改为等待店长确认。
- `backend/api/routers.py`、`backend/api/schemas.py`：新增预约批复提议和确认接口，并复用 Agent 任务表。
- `frontend/staff.js`：店长日程中可以生成并确认批复方案。
- `tests/test_appointment_approval.py`：覆盖待批复、员工拒绝、店长确认、客户同步、审计和重复确认。

#### 如何验证

- 预约批复、客户聊天预约、店长代约和预约调整相关测试共 `15 passed`。
- 测试确认客户接口创建的预约初始为 `pending`；店长确认后，客户接口读取为 `confirmed`，并存在“预约已确认”通知与 `agent.appointment_approved` 审计记录。
- `node --check frontend/staff.js` 和 `python -m compileall -q backend` 通过。

#### 面试怎么讲

> 预约审批不能只靠前端按钮，我把它设计成状态机：客户申请是 pending，店长确认后才变 confirmed。Agent 先生成可审查任务，确认时后端再用条件更新检查预约是否仍在 pending。这样可以防止重复批复和并发误操作，并且预约、通知和审计在同一事务里完成，三端读取同一份数据自然同步。

#### 下一步

给店长 Agent 增加语义路由和受控操作工具，让“帮我批复龙百川的预约”能够自动找到候选预约、生成批复任务并等待店长确认。

### 第 63 步：店长语义 Agent 批复预约（2026-07-15）

#### 本步改了什么

- 新增员工工作台意图样本库，包含日程、客户、会员、留存、护理知识、预约批复等业务意图及不同自然语言示例。
- 配置了 Chroma 时，意图先走独立的向量 collection；没有向量环境或向量服务不可用时，自动降级为本地 BM25，不会让员工助手直接失效。
- 语义检索只负责“用户想做什么”，不直接给数据库权限；日期和预约等明确业务结构、护理专业词等高确定性信号仍优先走对应受控工具，避免语义降级误路由。
- 店长说“帮我批复龙百川的预约”“把客户的预约审核通过”时，系统会查真实客户和待确认预约，生成确认任务，而不是让模型直接写数据库。
- 如果客户同名或有多条待确认预约，Agent 会列出候选并要求补充预约 ID；普通员工发送同类命令只得到权限拒绝说明，且不会修改数据。
- Agent 任务记录了原始指令、识别意图、检索方式、目标预约、确认结果和审计动作。

#### 为什么这样改

单纯关键词只能覆盖固定问法，直接把数据库写能力交给模型又不安全。这里把两件事拆开：向量检索帮助理解不同表达，后端工具和确认任务决定是否执行。这样既能理解自然语言，又不会因为一句相似但错误的话直接修改预约。

#### 和哪些模块有关

- `backend/agents/staff_intent.py`：意图样本、向量检索和 BM25 降级。
- `backend/agents/staff_graph.py`：查询 Agent 使用语义结果，同时保留高确定性业务护栏。
- `backend/api/routers.py`：店长自然语言批复路由、客户/预约候选解析、任务和审计。
- `backend/config.py`：增加员工意图向量检索开关。
- `frontend/staff.js`：店长 Agent 对话中展示批复确认卡片。
- `frontend/index.html`、`frontend/miniprogram/`：两端都读取同一 `/api/appointments`，因此不复制数据也能同步状态。

#### 如何验证

- `python -m pytest -q`：`92 passed`，仅有 1 条测试依赖弃用警告。
- `node --check frontend/staff.js`、`python -m compileall -q backend tests`、`git diff --check` 通过。
- 新增测试覆盖不同说法命中同一批复意图、普通员工不能批复、店长生成确认任务、确认后客户预约状态同步为 `confirmed`、客户通知和审计记录存在、重复确认被拒绝。
- 三端代码检查确认员工工作台、顾客 H5 和小程序均从 `/api/appointments` 读取预约状态。

#### 面试怎么讲

> 我把 Agent 拆成“语义理解层”和“受控执行层”。语义理解层用向量检索匹配业务意图，向量不可用时用本地 BM25 保证系统可运行；执行层不相信模型身份或模型参数，而是用登录令牌确认店长身份、从数据库重新定位预约、创建待确认任务，再用事务条件更新完成批复。这样自然语言更灵活，但写操作仍然可控、可审计、可回滚。

#### 下一步

继续把店长 Agent 的同一套“意图识别 → 真实对象定位 → 确认任务 → 事务执行 → 审计”模式扩展到改约、退款、服务核验和留存发送等其他高风险工作台操作。

### 第 61 步：增加员工代客预约并同步三端（2026-07-15）

#### 本步改了什么

- 在员工端“客户与会员”页面增加“帮客户预约”模块，可以选择已有客户、服务项目、发型师、可用时间和备注。
- 新增员工专用接口 `POST /api/staff/appointments`，只允许员工为已有客户创建预约。
- 员工代约复用原有 `AppointmentService.create_appointment`，继续使用时间槽原子占用，避免两个员工同时抢到同一时间。
- 预约成功后写入客户预约通知和员工操作审计。
- 预约成功后自动跳转到对应日期的“今日预约”，发型师分组下可以看到客户、服务和预约状态。
- 客户 H5 和小程序继续通过原有 `/api/appointments` 读取同一条预约，不额外复制数据。
- 修复了一处响应模型重名问题：代客预约使用独立的 `StaffBookingResponse`，避免被员工日程响应模型覆盖。
- 浏览器复核发现客户下拉框切换发型师时会重置客户，已补上客户选择状态同步，防止给错客户预约。

#### 为什么这样改

员工代客预约、客户自主预约和客户端展示必须只有一个数据源。如果员工端自己维护一份预约，客户端和发型师日程会出现不一致。现在员工只是换了一个入口，最终仍然写入同一张预约表、占用同一时间槽，并由三端分别读取。

#### 如何验证

- 新增 `tests/test_staff_booking.py`，验证员工代约成功、客户接口可见、客户收到预约通知、发型师日程出现客户记录和时间槽冲突返回 409。
- `python -m pytest tests/test_staff_booking.py tests/test_end_to_end_agent_flow.py -q`：2 passed。
- 浏览器实际选择李生日、张三老师和可用时间创建预约，页面跳转到今日预约，张三老师分组下显示李生日和剪发；成功提示显示“已为李生日预约张三老师，客户端已同步”。
- 截图保存到 `output/playwright/staff-assisted-booking.png`。
- `node --check frontend/staff.js`、`python -m compileall -q backend tests` 和 `git diff --check` 通过。

#### 面试怎么讲

> 员工代客预约没有另起一套业务逻辑，而是复用客户预约的时间槽和数据库服务。员工接口只增加客户 ID 和权限校验，成功后通过同一个预约事实同步到客户端和发型师日程，同时写通知和审计。这样可以避免三端各自保存预约造成数据不一致。

#### 下一步

后续可以把员工代约的服务项目改为后台可配置的服务套餐，并在正式接入微信时把预约通知接入真实模板消息；当前模拟环境已经完成业务闭环。

### 第 62 步：统一员工助手金额格式并完成回归（2026-07-15）

#### 本步改了什么

- 全量回归发现员工助手会员查询在模型路径下可能把 `68.00` 输出为 `68.0`。
- 会员余额、积分和动态等级现在优先采用受控 LangGraph 查询结果，保证数字格式和规则路径一致；模型仍可负责语言组织。
- 修复后全量测试恢复通过。

#### 为什么这样改

余额和会员等级是结构化业务数据，不能因为模型表达习惯不同而改变展示口径。模型可以润色文字，但金额、积分和等级必须由确定性查询结果控制。

#### 如何验证

- `python -m pytest -q`：89 passed，1 条依赖弃用警告。
- `python -m compileall -q backend migrations tests`、`node --check frontend/staff.js` 和 `git diff --check` 通过。
- 代客预约专项测试验证员工日程、客户预约查询、客户通知和时间槽冲突。

#### 面试怎么讲

> 对结构化数字我不会完全相信模型输出，而是让确定性 Graph 返回标准格式，模型只负责组织表达。这样金额不会出现 68.0 和 68.00 两种口径，动态会员等级也不会被模型改错。

### 第 63 步：把员工代客预约客户选择改为可输入搜索（2026-07-15）

#### 本步改了什么

- 客户选择从长下拉框改为可输入的搜索框，并通过 `datalist` 提供已有客户的姓名和手机号提示。
- 输入姓名、手机号或“姓名 · 手机号”后，前端会匹配客户资料并写入隐藏的客户 ID；切换发型师不会清空已经输入的客户。
- 输入不存在的客户时，提交会被拦截并提示“请输入并选择已有客户”，不会把手写文字当成客户 ID 发送给后端。
- 预约成功后清空客户搜索状态，避免下一次预约误带上上一个客户。
- 更新员工端脚本版本号，避免浏览器继续使用旧缓存。

#### 为什么这样改

真实门店客户数量会持续增长，164 个客户全部放在下拉框里，员工需要不断滚动查找，容易选错，也不适合后续扩展。可输入搜索更符合员工的工作习惯，但系统仍然只接受数据库里已经存在的客户，既提高效率又保持数据完整性。

#### 和哪些模块有关

- `frontend/staff.js`：负责输入匹配、隐藏 ID 同步和提交前校验。
- `frontend/staff.html`：更新静态资源版本，让浏览器加载最新脚本。
- `backend/api/routers.py`：继续接收并校验已有客户 ID，后端规则没有被前端绕过。
- 客户、预约和发型师日程：成功预约仍写入同一条预约事实，并同步三端。

#### 如何验证

- 输入客户姓名或手机号可以匹配已有客户，切换发型师后输入内容仍保留。
- 输入不存在的客户时，点击创建预约不会发起请求，并显示明确提示。
- 选择已有客户、发型师和时间后可以正常创建预约。
- `node --check frontend/staff.js`、`python -m compileall -q backend migrations tests`、`python -m pytest tests/test_staff_booking.py -q` 和 `git diff --check` 通过。
- 全量 `python -m pytest -q` 结果为 `88 passed, 2 failed`；失败项是此前权限分层对应的旧测试，普通发型师调用店长专用 Agent 操作时按设计返回 403，与本步输入框改动无关。

#### 面试怎么讲

> 客户很多时，我没有继续堆一个超长下拉框，而是做成“可输入、必须匹配已有客户”的搜索选择。前端负责提升查找效率，匹配后只提交数据库客户 ID；后端仍然做最终校验。这样员工操作更快，同时不会因为手写姓名或手机号产生不存在的客户预约。

### 第 64 步：修正服务核验后的预约与员工记录同步（2026-07-15）

#### 本步改了什么

- “核验服务”改为右侧滑动窗口，点击预约行后不会把核验表单追加到长页面底部。
- 核验窗口支持关闭按钮和 `Esc`，与其他长数据详情抽屉保持一致。
- 核验提交成功后，前端重新读取今日预约和员工概览，预约行立即显示“已核验”，员工管理不再停留在旧数据。
- 员工管理的核验记录增加发型师 ID，并按发型师分组显示客户、服务、核验金额、核验时间和状态。
- 保留“已核验”和“已完成”的业务边界：核验只是确认到店并建立服务记录；完成服务后才扣套餐、记消费、加积分和计入正式绩效。
- 增加回归断言，确认核验记录归属于正确的发型师。

#### 为什么这样改

之前后端已经写入核验记录，但前端只刷新了核验窗口，没有刷新预约和员工概览，所以员工会看到“按钮点了但状态没变”。同时，所有核验记录放在一个总表里，员工无法快速确认某条记录属于哪个发型师。现在一次核验成功后刷新两类工作数据，并使用发型师 ID 做归档，页面状态和数据库事实保持一致。

#### 如何验证

- 点击预约行的“核验服务”会打开右侧窗口。
- 提交核验后，预约状态变为“已核验”，员工管理对应发型师下出现该客户记录。
- `python -m pytest tests/test_staff_overview.py tests/test_staff_booking.py -q`：5 passed。
- `node --check frontend/staff.js`、`python -m compileall -q backend migrations tests` 和 `git diff --check` 通过。

#### 面试怎么讲

> 核验接口写库成功后，我不会只更新当前弹窗，而是重新读取预约和员工概览，让两个工作入口都反映同一条服务事实。核验和完成分开，既能让员工及时看到客户已到店，又避免在服务尚未结束时提前计入营业额和绩效。

### 第 65 步：店长 Agent 的语义改约与预约批复权限收口（2026-07-15）

> 说明：本节记录的是上一版“员工不能直接批准”的方案；根据后续业务决定，已由第 66 步改为员工可以直接批准客户预约。

#### 本步改了什么

- 修正了一个真实权限漏洞：旧的“直接批准预约”接口曾允许普通员工调用；现在只有店长账号 `19158260000` 对应的 `ADMIN` 角色可以调用，普通员工即使绕过页面直接请求接口也会收到 403，并留下权限拒绝审计。
- 员工端日程表不再为普通员工显示“批准预约”按钮；店长仍可在工作台上处理待确认预约。
- 店长 Agent 新增自然语言改约。例如“把自然改约客户的预约改到 2026-07-16 15:00”，系统会从真实客户、预约和时间槽数据中查找目标，生成“原预约 -> 新预约”的待确认方案。
- 当客户有多条预约、时间不完整或没有空档时，Agent 只会追问需要的信息，不会猜测客户、发型师或时间槽。
- 改约确认时仍复用原来的事务工作流：重新检查预约状态、目标时间槽是否被占用，再写预约、客户通知和审计记录；因此生成方案本身不会修改数据。

#### 为什么这样改

语义检索的职责是理解“改约”和“调整预约”这些不同说法，不是获得写数据库的权限。真正的业务操作必须先定位到唯一的真实记录，再让店长确认，最后由后端重新校验后执行。这样既能让 Agent 理解自然表达，也不会因为它理解错一句话就改错客户的预约。

#### 和哪些模块有关

- `backend/agents/staff_intent.py`：用语义检索识别预约调整意图。
- `backend/api/routers.py`：解析客户、日期时间和可用时间槽，生成待确认任务，并在接口层强制店长权限。
- `backend/agents/appointment_change_graph.py` 与 `backend/database/appointment_change.py`：确认时执行并发校验、预约调整、客户通知和审计。
- `frontend/staff.js`：员工隐藏写操作入口；店长 Agent 显示改约前后信息并提供确认按钮。
- `tests/test_appointment_approval.py`：覆盖员工越权拒绝、店长语义改约、确认后同步和旧时间槽释放。

#### 如何验证

- `python -m pytest tests/test_appointment_approval.py tests/test_appointment_change.py -q`：`10 passed`。
- 验证普通员工请求直接批复接口返回 403；店长先批准待确认预约，再通过自然语言生成改约任务，确认后预约切换到新时间槽，旧时间槽恢复可用。
- `python -m compileall -q backend tests`、`node --check frontend/staff.js` 和 `git diff --check` 通过。

#### 面试怎么讲

> 我把“理解一句话”和“执行一次修改”拆开了。向量检索负责把“改约、改到、调整预约”等近义说法路由到改约工作流，但它只能查真实客户、预约和时间槽。只有唯一目标存在时才生成方案，店长确认后后端还会重新检查目标时间槽和预约状态，最后把预约、通知、审计放在同一条受控链路里。普通员工在前端看不到写操作，直接调接口也会被后端拒绝。

#### 下一步

继续把服务完成和留存消息发送这两类会影响消费、套餐次数或客户触达的动作接入同样的“Agent 提议 -> 店长确认 -> 高风险密码复核（如需要） -> 事务与审计”模式，并做全量回归。

### 第 66 步：收口预约批复入口并核对三端同步契约（2026-07-15）

#### 本步改了什么

- 工作台日程里的“批准预约”不再直接改数据库。店长点击后只会生成一条 Agent 批复方案，核对客户、时间、项目和发型师后还要点击“确认批复”，预约才会从 `pending` 变为 `confirmed`。
- 旧的直接批准接口保留为受保护的兼容地址，但对店长也返回 `410`，明确要求走“Agent 任务 -> 店长确认”的受控流程，避免通过接口绕过二次确认。
- 小程序开发配置从本地 `8000` 对齐到当前项目的 `8001` 服务；客户 H5、小程序和员工工作台都读取同一个 `/api/appointments` 数据源。
- 新增三端同步契约测试，检查客户 H5 与小程序都使用共享预约接口，避免某一端未来误改成独立缓存或独立数据表。

#### 为什么这样改

对于门店来说，批复预约不是普通的页面按钮，而是会影响客户到店、发型师档期和客户通知的业务决定。改约已经有“先提议再确认”，批复也必须保持同样的交互，店长不会因为一次误点直接改变客户状态。

三端同步不应该靠“复制一份预约数据”实现。员工端操作同一条预约事实，客户 H5 和小程序再从同一 API 读取结果，才能避免一个端显示已确认、另一个端还显示待确认。

#### 和哪些模块有关

- `frontend/staff.js`：批复按钮先请求 Agent 任务，再显示确认区。
- `backend/api/routers.py`：停用直接批复写入路径，只允许 Agent 确认任务执行状态变更、客户通知和审计。
- `frontend/index.html`、`frontend/miniprogram/app.js`：客户两端读取共享预约接口；小程序本地开发目标与当前服务统一。
- `tests/test_appointment_approval.py`、`tests/test_client_sync_contract.py`：验证员工拒绝、店长任务确认和三端 API 契约。

#### 如何验证

- `python -m pytest tests/test_appointment_approval.py tests/test_client_sync_contract.py -q`：`6 passed`。
- 自动化验证覆盖：普通员工调用旧批复接口返回 403；店长调用旧直写接口返回 410；店长通过提议和确认任务后，客户预约状态变为 `confirmed`，客户通知可查询。
- `node --check frontend/staff.js`、`node --check frontend/miniprogram/app.js`、`python -m compileall -q backend tests` 和 `git diff --check` 通过。
- 本机 Playwright 被应用控制策略阻止，未把浏览器截图当作验收证据；本步使用接口端到端测试和前端静态契约验证替代。

#### 面试怎么讲

> 我把“批复预约”当作一个需要复核的业务决策，而不是普通按钮事件。前端不再直接请求写接口，而是让后端先生成 Agent 任务；店长复核目标信息后确认，后端才在当前状态仍为 pending 时执行。客户 H5 和小程序不保存自己的预约副本，都读取同一个预约 API，所以批复后能看到同一份状态和通知。

#### 下一步

继续将服务完成、留存发送等外部影响更强的动作纳入同样的 Agent 任务和确认链路，并在真实微信环境中将小程序 API 地址替换为 HTTPS 的局域网或生产域名。

### 第 67 步：服务完成增加店长密码复验与 Agent 执行链路（2026-07-15）

#### 本步改了什么

- 修复了服务完成接口的安全回退：之前页面有店长密码输入框，但前端没有提交、后端也没有要求密码；现在密码为空或错误都会被拒绝。
- 密码验证失败会生成 `security.step_up_denied` 审计记录，且服务仍保持“已核验”，不会扣套餐、生成消费、增加积分或计入绩效。
- 店长 Agent 支持“完成某客户的护理服务”这类指令。它先定位唯一的已核验服务，展示客户、项目、金额、套餐和剩余次数，再要求店长密码和确认。
- Agent 确认时再次检查服务状态和套餐可用性；服务完成、套餐扣次、消费交易、积分、绩效审计和 Agent 审计在同一数据库事务内提交。

#### 为什么这样改

服务完成是门店里风险很高的动作，因为它不仅改变预约状态，还会影响客户套餐余额、消费金额、会员积分和员工绩效。只凭登录态不够，必须让店长再次证明当前操作意图，所以使用密码复验加确认任务。

#### 和哪些模块有关

- `backend/api/schemas.py`：服务完成请求必须带店长密码。
- `backend/api/routers.py`：抽取受控完成服务逻辑，复用给页面接口和 Agent 任务；密码失败和完成成功都会审计。
- `frontend/staff.js`：页面完成服务会提交店长密码；Agent 任务展示套餐扣次影响并要求密码确认。
- `tests/test_service_completion_security.py`：覆盖密码失败不写入、审计记录、自然语言提议和确认完成。

#### 如何验证

- `python -m pytest tests/test_service_completion_security.py tests/test_staff_overview.py -q`：`6 passed`。
- 验证错误密码返回 403，核验服务仍为 `verified`；正确密码确认后状态变为 `completed`，并存在 `agent.service_completion_confirmed` 审计。
- `node --check frontend/staff.js`、`python -m compileall -q backend tests` 和 `git diff --check` 通过。

#### 面试怎么讲

> 服务完成不是简单改一个状态，它会同时影响套餐、收入、积分和绩效。我把它设计成二次认证的高风险操作：Agent 只能生成包含金额和套餐影响的方案，店长输入密码并确认后，后端重新校验服务和套餐状态，再在一个事务里完成所有数据变更和审计。密码错误时只留下安全审计，不会产生任何业务写入。

#### 下一步

将留存消息发送也改造成可预览、可确认的 Agent 任务，并继续用店长、员工、客户三种角色做端到端验收。

### 第 68 步：留存消息改为 Agent 预览后确认发送（2026-07-15）

#### 本步改了什么

- 店长 Agent 支持“给某客户发送回访消息”。它会先定位唯一的可发送留存任务，并展示最终要发送的话术；没有确认时，不会建立触达记录，也不会向客户发送通知。
- 店长确认后才调用消息通道。成功发送会写入联系记录、客户站内通知、冷却期和审计；失败时保留失败状态，支持后续重试。
- 如果同一客户存在多条待处理留存任务，Agent 不会自己选一条，而是返回任务编号让店长补充；这避免把生日、复购和流失提醒混淆。
- 普通员工通过 Agent 发起发送指令会被拒绝，只能查询；店长可以审阅具体文案后确认发送。

#### 为什么这样改

留存消息是对外触达，即使它不直接改金额，也会影响客户体验和退订合规。Agent 可以帮店长找到任务和组织话术，但不能把“识别到客户”直接等同于“允许发送”。预览和确认让店长对真正发出的内容保有最后决定权。

#### 和哪些模块有关

- `backend/api/routers.py`：新增留存任务定位、文案提取、Agent 提议和确认发送；将消息发送函数改为支持和 Agent 任务同一事务提交。
- `frontend/staff.js`：Agent 面板显示待发送客户与最终文案，并提供确认发送或取消。
- `tests/test_retention_send_agent.py`：验证未确认不发送、确认后进入冷却、客户可读通知和审计存在。

#### 如何验证

- `python -m pytest tests/test_retention_send_agent.py tests/test_retention_workbench.py tests/test_service_completion_security.py -q`：`12 passed`。
- 验证“给 Agent 留存客户发送回访消息”先返回 `retention_send` 待确认任务；确认后任务状态为 `cooling`，客户通知中出现“留存提醒”，审计中存在 `agent.retention_send_confirmed`。
- `node --check frontend/staff.js`、`python -m compileall -q backend tests` 和 `git diff --check` 通过。

#### 面试怎么讲

> 留存发送属于外部副作用操作。我让 Agent 只负责定位任务和生成预览，真正发送前必须店长确认。确认阶段后端还会重新检查客户是否在冷却期、是否退订、任务是否已被其他人处理。发送结果、客户通知和审计跟任务状态一起写入，失败也会留下事实记录而不是假装发送成功。

#### 下一步

继续核对店长 Agent 已覆盖的高风险操作，并把仍需表单参数的代客预约、套餐配置等操作保留为受权限保护的工作台表单，直到补齐清晰的自然语言参数收集流程。

### 第 69 步：代客预约改为单一事务，避免三端半同步（2026-07-15）

#### 本步改了什么

- 修复店长代客预约的事务边界：以前预约和时间槽会先提交，再单独写客户通知和审计；现在四项数据在同一事务内提交。
- 任一步失败时，预约、时间槽占用、客户通知和审计会一起回滚，不会出现客户看到预约但没有通知、或时间槽被占用但没有可追溯记录的半完成状态。
- 客户自主预约继续保持原有逻辑；店长代客预约显式使用不自动提交的底层服务，等通知和审计准备完成后统一提交。

#### 为什么这样改

三端同步的关键不是“分别刷新三个页面”，而是三端读取的数据必须是一条完整业务事实。代客预约只要先提交预约，后面任何一步异常都会破坏这个事实。把相关写入放到同一个事务里，才可以保证要么全部成功，要么用户什么都看不到。

#### 和哪些模块有关

- `backend/database/service.py`：预约创建支持由调用方控制是否立即提交。
- `backend/api/routers.py`：店长代客预约关闭底层自动提交，统一写客户通知和审计后再提交。
- `tests/test_staff_booking.py`、`tests/test_booking_concurrency.py`：覆盖店长代约、客户可见、员工日程、通知和时间槽冲突。

#### 如何验证

- `python -m pytest tests/test_staff_booking.py tests/test_booking_concurrency.py tests/test_customer_api.py -q`：`5 passed`。
- 验证店长代约后，客户预约列表、员工日程和客户通知均能读取同一预约编号；再次抢占相同时间槽返回 409。
- `python -m compileall -q backend tests` 和 `git diff --check` 通过。

#### 面试怎么讲

> 我把代客预约看成一个完整业务事务，不是单独插入一条预约。时间槽占用、预约、客户通知和审计必须一起提交，任何一步出错就整体回滚。这样工作台、客户 H5 和小程序读到的永远是完整事实，而不是靠前端补救同步。

#### 下一步

继续以真实店长、员工、客户角色审查剩余表单操作的风险等级；对涉及外部触达、金额、套餐扣次和状态流转的动作保持提议、确认和审计模式。

### 第 70 步：堵住员工端运营与余额数据旁路（2026-07-15）

#### 本步改了什么

- 生日营销接口和新版留存任务接口改为仅店长可读。它们包含客户余额、留存证据和运营策略，不属于普通员工的简单查询范围。
- 员工工作台登录后不再请求这三类店长数据，避免浏览器网络层收到敏感字段，也避免页面后台不断产生无意义的 403 请求。
- 保留普通员工的日程、客户基础资料、会员等级积分、护理知识等查询能力；员工尝试访问店长数据会被拒绝并写入权限审计。

#### 为什么这样改

隐藏导航菜单并不能保护数据，只要接口仍返回内容，员工可以在浏览器开发工具或脚本中直接读取。权限必须由后端决定，前端同时不发起不需要的请求，才能既保证安全又让页面运行干净。

#### 如何验证

- `python -m pytest tests/test_employee_data_boundaries.py tests/test_chat_agent.py tests/test_retention_workbench.py -q`：`18 passed`。
- 普通员工访问生日营销和留存任务均返回 403；店长可正常读取；审计日志中记录了两条被拒绝路径。
- `node --check frontend/staff.js`、`python -m compileall -q backend tests` 和 `git diff --check` 通过。

#### 面试怎么讲

> 我做权限不是只在页面上隐藏按钮。员工端连敏感接口都不请求，后端也会根据 JWT 角色拒绝直接调用，并记录拒绝审计。这样即使有人绕过页面，仍拿不到客户余额和运营策略数据。

### 第 66 步：员工直接批准客户预约（2026-07-15）

#### 这一步改了什么

- 新增 `POST /api/staff/appointments/{appointment_id}/approve`，允许已登录员工直接批准待处理预约。
- 点击“批准预约”后，预约从 `pending` 变为 `confirmed`，同时创建客户通知和员工审计记录。
- 前端增加“批准中...”反馈，成功后刷新今日预约；重复点击会被后端拒绝，不会重复通知客户。
- 原来的 Agent 批复接口保留给自然语言助手使用，但员工工作台不再绕过员工操作去创建 Agent 批复任务。

#### 为什么这样改

客户预约的批准是员工明确、低歧义的业务动作，不需要先生成 Agent 方案再二次确认。后端仍然使用“只有状态还是 pending 才能更新”的条件，保证两个员工同时点击时只有一个能成功。

#### 如何验证

- `python -m pytest tests/test_appointment_approval.py -q`
- `node --check frontend/staff.js`
- `python -m compileall -q backend migrations tests`

#### 面试怎么讲

> 预约批准由员工直接操作，Agent 不参与低风险的确定性状态变更。接口在数据库层用 pending 条件做幂等校验，成功后在同一事务里更新预约、通知客户并写审计日志；前端刷新今日预约，所以员工、客户和发型师看到的是同一个 confirmed 状态。

### 第 67 步：服务核验和完成取消额外密码（2026-07-15）

#### 这一步改了什么

- 移除服务完成弹窗中的“店长密码”输入框。
- 完成服务接口不再校验额外密码，但仍要求已登录管理员权限。
- 保留服务状态校验、重复完成保护、套餐扣次、消费流水、积分、正式绩效和审计记录。
- 退款审批等其他高风险操作仍保留店长密码，不受本次修改影响。

#### 为什么这样改

门店已经通过员工工作台登录身份确认操作人，服务核验和完成服务属于同一条现场服务流程，不需要员工在弹窗里再次输入密码。真正影响数据的扣次、消费和绩效逻辑仍由后端事务统一执行。

#### 如何验证

- `python -m pytest tests/test_staff_overview.py -q`
- `node --check frontend/staff.js`
- `python -m compileall -q backend migrations tests`

#### 面试怎么讲

> 我把登录权限和重复密码校验分开。完成服务接口仍然只允许有权限的员工调用，并在事务里校验核验状态、套餐状态和是否已有消费记录；密码输入只是交互层的二次校验，本次按门店流程移除，但审计和幂等保护仍然保留。
### 第 69 步：服务完成同步余额、总览和员工绩效（2026-07-15）

#### 这一步改了什么

- 新增统一的钱包消费方法：直接消费会减少 `wallet_accounts.balance_cents`，并新增 `wallet_transactions` 的 `purchase` 扣款流水。
- 套餐服务只扣 `customer_packages.remaining_uses`，不会再次扣钱包余额；服务价值仍会写入消费记录，用于员工绩效和服务统计。
- 服务完成、积分、消费记录、余额流水和审计在同一个数据库事务中提交。余额不足时返回 409，预约不会完成，也不会产生部分业绩。
- 完成服务后，员工端重新加载预约、客户、钱包和员工总览数据，页面显示与数据库保持一致。

#### 为什么这样改

余额是账户当前状态，流水是不可替换的历史凭证，消费记录是服务和绩效统计来源。三者必须同时变化，才能回答“扣了多少钱、什么时候扣的、谁完成的、员工绩效为什么增加”。套餐已经在购买时付款，核销时重复扣钱包会造成重复收费，所以只扣次数。

#### 如何验证

- 客户充值 120 元，完成 88 元直接消费后，余额为 32 元，钱包出现 88 元 `purchase` 流水，今日消费和员工绩效为 88 元。
- 余额只有 20 元时完成 88 元服务返回 409，余额、预约状态、消费记录和绩效都不改变。
- `python -m pytest tests/test_staff_overview.py tests/test_service_completion_security.py tests/test_appointment_approval.py -q`：13 passed。
- `node --check frontend/staff.js`、`python -m compileall -q backend migrations tests` 和 `git diff --check` 通过。

#### 面试怎么讲

> 我把服务完成设计成一个事务，而不是只把预约状态改成 completed。直接消费会同时扣钱包并写消费流水，套餐消费只扣次数；随后写服务消费、积分、员工绩效和审计。余额不足时整个事务回滚，前端成功后重新拉取客户余额和员工总览，所以页面和账务数据不会出现不一致。
### 第 70 步：修复服务完成按钮无响应（2026-07-15）

#### 这一步改了什么

- 服务核验完成只使用当前已登录的管理员权限，不再要求页面中不存在的店长密码输入框。
- 普通服务完成接口和 Agent 服务完成确认接口都可以直接提交确认；退款审批仍保留额外密码复核。
- 保留余额不足校验、消费流水、积分、员工绩效、审计和事务回滚。

#### 如何验证

- 无密码调用服务完成接口可以成功完成服务。
- Agent 确认服务完成时不填写密码也可以成功。
- `python -m pytest tests/test_service_completion_security.py tests/test_staff_overview.py -q`、`node --check frontend/staff.js` 和 `python -m compileall -q backend migrations tests` 通过。

### 第 71 步：店长通过员工助手操作工作台并批量发送生日提醒（2026-07-16）

#### 这一步改了什么

- 员工助手仍然允许普通员工查询门店信息，但识别到批复预约、改约、退款、完成服务、发送留存提醒等写操作时，后端会拒绝普通员工，并写入权限审计。
- 只有 `admin` 角色的店长账号可以进入这些 Agent 工作流；任务查询和任务确认也继续由后端校验，不能靠前端隐藏按钮来保护。
- 店长输入“帮我把留存提醒里的生日提醒全部发送”时，系统会筛选生日类型且处于待处理或发送失败状态的任务，逐条调用已有发送流程。
- 每条发送成功后会进入冷却状态并创建客户通知；单条失败会记录失败原因并继续处理其他客户，最后返回成功数、失败数和失败客户。
- 前端员工助手补充了批量生日提醒示例，并把店长 Agent 标记为可以查询和操作工作台。

#### 为什么这么改

“查询权限”和“写操作权限”不是一回事。员工可以知道今天有哪些预约，但不能通过一句自然语言直接批复预约或向客户发消息，所以权限判断必须放在后端业务路由中。批量发送复用单条发送函数，可以保证冷却、客户通知和发送审计的规则只有一套，避免批量逻辑绕过原有保护。

#### 和哪些模块有关

- `backend/api/routers.py`：员工助手权限拦截、生日任务筛选、逐条发送和批量审计。
- `backend/database/models.py`：使用 `UserRole.ADMIN`、`ReminderType.BIRTHDAY` 和留存任务状态。
- `frontend/staff.js`：员工助手提示语和店长操作状态展示。
- `tests/test_retention_send_agent.py`、`tests/test_appointment_approval.py`：验证普通员工不能操作、店长可以操作以及生日任务批量发送。

#### 如何验证

- `python -m pytest tests/test_retention_send_agent.py tests/test_appointment_approval.py tests/test_appointment_change.py tests/test_service_completion_security.py tests/test_refund_agent.py tests/test_employee_data_boundaries.py tests/test_staff_graph.py -q`：33 passed。
- `node --check frontend/staff.js` 通过。
- `python -m compileall -q backend tests` 通过。
- 生日批量测试确认只发送生日任务，复购任务不改变；生日任务进入 `cooling`，客户收到“留存提醒”，并生成批量审计记录。

#### 面试怎么讲

> 我把员工助手分成查询路径和工作台写操作路径。普通员工可以使用只读 Agent，店长角色才能进入批复、改约、退款、服务完成和留存发送等工作流。对于批量生日提醒，我先按提醒类型和可发送状态筛选任务，再逐条复用单条发送函数，每条任务独立落库，最后返回批量结果并写审计。这样既支持自然语言操作，又不会因为一条发送失败而把其他客户的结果混在一起。

#### 下一步

继续审核工作台中尚未接入自然语言 Agent 的表单型操作，明确哪些操作需要“先提议、再确认”，哪些低风险操作可以直接执行。

### 第 72 步：补齐员工助手的工作台操作覆盖（2026-07-16）

#### 这一步改了什么

- 店长可以通过员工助手用自然语言为已有客户创建预约。系统会识别客户、服务、日期、时间和发型师，找不到唯一时间槽时会先要求补充信息，不会盲目创建。
- 店长可以通过员工助手完成留存任务的转人工、忽略、记录客户回复和关闭，并复用原有留存状态、抑制规则和审计逻辑。
- 店长可以通过员工助手核验服务；使用套餐时继续校验套餐归属、服务匹配和有效期，直接消费时要求提供金额。核验完成和服务完成仍然是两个步骤。
- 普通员工对这些新增写操作仍然会被后端拒绝，只能继续使用查询和护理知识 Agent。
- 修复了自然语言路由的两个回归：改约指令不会误进入代约，护理知识问题不会被误判为预约操作。

#### 为什么这么改

员工助手不能只依赖一个模糊的意图分类结果。分类器负责提供候选意图，真正执行前还要检查业务关键词和必要参数；只有参数完整且用户是店长时，才调用已有工作台业务函数。这样可以避免一句知识问题触发写操作，也能让代约、留存和服务核验继续使用原来的事务和权限保护。

#### 和哪些模块有关

- `backend/api/routers.py`：新增 Agent 参数解析、代约、服务核验和留存状态操作路由。
- `backend/agents/staff_intent.py`：继续提供自然语言意图候选，不直接授予权限。
- `frontend/staff.js`：补充店长 Agent 的代约和留存操作示例。
- `tests/test_staff_agent_workbench_operations.py`：验证店长可以执行新增操作，普通员工不能代约。

#### 如何验证

- `python -m pytest tests/test_retention_send_agent.py tests/test_staff_agent_workbench_operations.py tests/test_appointment_approval.py tests/test_appointment_change.py tests/test_service_completion_security.py tests/test_refund_agent.py tests/test_employee_data_boundaries.py tests/test_staff_graph.py tests/test_staff_booking.py -q`：39 passed。
- `node --check frontend/staff.js` 通过。
- `python -m compileall -q backend tests` 通过。
- `git diff --check` 通过；测试中仅有现有 `httpx` 弃用警告，没有业务失败。

#### 面试怎么讲

> 我没有让大模型直接决定是否能操作数据库。Agent 先识别意图，再由后端根据用户角色、业务关键词和结构化参数做二次校验。代约复用时间槽占用逻辑，服务核验复用套餐和金额校验，留存管理复用原有状态机；这样自然语言只是操作入口，真正的业务规则仍然由后端掌控。

#### 下一步

本次约定的店长 Agent 工作台操作已经接入并通过回归测试；后续新增模块时继续沿用“意图识别、权限校验、参数补齐、复用业务函数、审计”的流程。

### 第 73 步：修复生日提醒批量指令的自然语言识别（2026-07-16）

#### 这一步改了什么

- 生日提醒发送现在支持“全部、所有、批量、一键、一起、都、全量”等表达，不再要求用户使用固定关键词。
- 只要指令包含生日提醒发送动作，且没有客户姓名、手机号或任务编号，就默认批量发送全部可发送的生日任务。
- 如果指令明确指定客户，仍然进入单条留存发送确认流程，避免把单个客户请求误发给所有人。
- 增加了截图中的原句“帮我把留存提醒里的生日提醒一起发送一下”回归测试。

#### 为什么这么改

Agent 的价值是理解用户意图，而不是要求用户背固定命令。批量和单条的真正区别是“有没有指定目标”，所以后端用客户身份、手机号和任务编号做目标判断，再结合生日和发送动作决定路由。

#### 如何验证

- 生日提醒专项测试：`4 passed`。
- 完整相关回归测试：`40 passed`。
- `node --check frontend/staff.js`、`python -m compileall -q backend tests` 和 `git diff --check` 通过。

#### 面试怎么讲

> 我把自然语言路由从固定关键词匹配改成了目标感知：如果用户说的是生日提醒发送，但没有指定客户，就按批量任务处理；如果出现客户姓名、手机号或任务编号，就保留单条确认流程。这样既降低了使用门槛，也避免批量发送误伤单个客户请求。

### 第 74 步：接入店长 Agent 通用工作台规划和按到店天数批量发送（2026-07-16）

#### 这一步改了什么

- 新增 `StaffOperationPlan` 结构化计划。它把店长原话整理成动作、单人或批量范围、客户/任务参数、消息内容和是否需要确认，但自己不执行数据库操作。
- 有可用 LLM 时，未知表达会请求模型输出 JSON 计划；模型不可用、格式不支持或请求失败时，自动回退到本地规则解析。已明确模块但缺少方向时直接澄清，避免无意义的外部调用。
- 员工助手入口现在会先经过动作白名单和后端角色校验。店长可以通过自然语言触发留存扫描、旧版提醒的“已联系/忽略”、已有的预约、服务、退款和留存任务流程；普通员工仍然只能查询，不能写入工作台。
- 支持“给两百天以上未到店的人员发消息：……”这类人群指令：按 `User.last_visit` 计算真实人群，运行留存扫描生成任务，再逐条复用 `_send_retention_task`。每条任务独立记录发送状态、客户通知、失败原因和审计，批量结束后返回汇总。
- 模糊的工作台指令会返回“我理解你的意思是……”和缺少的确认信息，不会修改预约、退款、服务或留存业务数据。

#### 为什么这么改

大模型适合做自然语言理解，不适合直接决定数据库状态。现在的边界是：规划器负责把口语变成结构化意图，路由负责权限和参数校验，工作台已有业务函数负责真正写入。这样 Agent 和页面按钮使用同一套状态机、通知、钱包/套餐逻辑和审计逻辑，不会出现“按钮能做、Agent 做出另一套结果”的问题。

#### 和哪些模块有关

- `backend/agents/staff_operation_planner.py`：动作白名单、本地解析、LLM 规划提示词和中文天数识别。
- `backend/agents/langchain_agent.py`：结构化规划入口、普通 JSON 解析和 LLM 失败降级。
- `backend/api/routers.py`：店长权限拦截、工作台能力分流、按 `last_visit` 解析人群和逐条发送。
- `backend/database/retention.py`：留存扫描、抑制、冷却和任务状态规则。
- `tests/test_staff_agent_workbench_operations.py`：200 天批量发送、模糊请求、权限和既有操作回归。

#### 如何验证

- 员工助手、留存、退款、预约批复、服务完成专项：`28 passed`。
- 聊天 Agent：`8 passed`。
- 其他 API、认证、并发、迁移、工作台和钱包测试：`21 passed`、`56 passed`。
- 合计收集的 `113` 项测试全部通过；仅有现有 `httpx` 弃用警告。
- `python -m py_compile backend/agents/staff_operation_planner.py backend/agents/langchain_agent.py backend/api/routers.py` 通过。
- `git diff --check` 通过。

#### 面试怎么讲

> 我把店长 Agent 设计成“规划器 + 能力白名单 + 原业务服务”的三层结构。LLM 只输出结构化操作计划，后端先校验当前用户是不是店长、目标是否唯一、参数是否真实，再调用和页面按钮相同的业务函数。批量触达会按 `last_visit` 找出人群，逐条复用单任务发送，所以任务状态、客户通知、失败重试和审计都保持一致；高风险操作先生成确认方案，模糊指令则先把我的理解展示出来，不会直接改数据。

#### 运行态验收

- 已重启开发服务：`http://127.0.0.1:8001/staff` 返回 HTTP 200。
- 已用店长账号 `19158260000` 登录，页面显示“管理员”，侧栏可见工作概览、预约、客户、员工、退款、留存、审计和员工助手模块。
- 已通过真实浏览器打开员工助手并执行“查看今天预约”，返回受控数据 `2026-07-16共有 0 条预约`。
- 已修复模型回复日期与真实系统日期不一致时的覆盖问题，日程优先使用受控 Graph 的真实结果。

#### 下一步

后续新增工作台按钮时，继续在动作白名单中登记能力，并补充“自然语言规划 -> 权限 -> 参数 -> 原业务函数 -> 审计”的回归测试。

### 第 75 步：修复“发一次消息”识别并给员工助手消息区加滚动（2026-07-16）

#### 这一步改了什么

- 员工助手现在识别“发一次消息、发一条消息、联系一下、发短信”等发送说法，不会再把它误判成只查询留存提醒。
- “给超过200天没来的客户发一次消息”会进入按 `last_visit` 筛选的批量发送能力，发送成功后任务进入冷却状态，并创建客户通知。
- 员工助手消息列表增加最大高度和内部纵向滚动；长客户名单或长回复只在助手面板内滚动，不再把整个工作台页面无限撑长。

#### 为什么这么改

自然语言里动作词会插入数量词，例如“发一次消息”，不能只匹配固定的“发送”。这类误判会让系统返回查询结果，但不执行用户想要的发送动作。消息区使用独立滚动则把长内容和工作台布局隔离，保持顶部、侧栏和输入框稳定。

#### 和哪些模块有关

- `backend/agents/staff_operation_planner.py`：扩充发送动作同义词。
- `frontend/staff.css`：为 `.message-list` 增加最大高度、纵向滚动和滚动边界。
- `tests/test_staff_agent_workbench_operations.py`：用“超过200天没来的客户发一次消息”原句验证真实发送结果。

#### 如何验证

- 200 天批量发送专项测试：`1 passed`。
- 规划器原句断言确认动作是 `retention.send`、天数是 `200`、范围是 `batch`。
- `node --check frontend/staff.js` 和 `git diff --check` 通过。

#### 面试怎么讲

> 这次修复分为语义层和界面层。语义层把“发一次消息”等口语归一到同一个发送能力，再由后端按客户到店时间筛选和逐条复用发送事务；界面层给消息列表设置局部滚动，避免长结果影响整个运营工作台布局。

### 第 76 步：修正批量发送统计口径和工作台刷新（2026-07-16）

#### 这一步改了什么

- 批量发送现在区分“命中客户人数”“可发送任务数”“成功数”“失败数”和“跳过数”，不再把原始人群数量和实际可发送任务数量混在一起。
- 200 天流失筛选只统计有匹配流失任务的客户；已经冷却、已发送、转人工或忽略的任务会列入跳过原因，不会重复发送。
- 员工助手完成批量发送、留存扫描、代约、服务核验等直接写操作后，会自动重新加载工作台数据，留存列表会立即显示最新状态。
- 更新 `staff.css` 和 `staff.js` 的资源版本号，避免浏览器继续使用旧缓存导致滚动和自动刷新看起来没有生效。

#### 这一步查到的真实情况

- 当时 8 人是流失任务列表中的 8 位客户。
- 代码按 `last_visit` 原始筛选得到 9 人，其中多出的顾明远是生日主任务，不属于本次流失发送任务。
- 9 人中 2 人已有冷却状态，实际本次可发送 7 条，7 条都成功并进入 `cooling`，审计和客户通知均已存在。

#### 为什么这么改

批量操作必须让“筛选到多少人”和“真正执行了多少条”可解释、可审计。页面也不能只显示 Agent 回复而保留旧的待处理数据，否则用户会误以为发送失败或重复发送。现在回复会直接说明跳过对象和原因，前端随后刷新真实后端状态。

#### 和哪些模块有关

- `backend/api/routers.py`：批量任务筛选、跳过原因和汇总回复。
- `frontend/staff.js`：Agent 直接写操作成功后的工作台数据刷新。
- `tests/test_staff_agent_workbench_operations.py`：验证一个可发送客户和一个已冷却客户的统计与状态。

#### 如何验证

- 员工助手相关回归：`28 passed`。
- 新增口径测试确认命中 2 人、可发送 1 条、跳过 1 人，已冷却任务没有重复通知。
- `python -m py_compile`、`node --check frontend/staff.js` 和 `git diff --check` 通过。
### 第 77 步：智能助手名称、最新消息滚动和已发送留存过滤（2026-07-16）
#### 这一步改了什么？
- 员工助手模块统一改名为“智能助手”，包括侧栏、页面标题和店长面板标题。
- 消息列表增加自动滚动：发送问题后先定位到最新的用户消息，收到回答后再次定位到最新回复，不需要手动向下拖动。
- 智能助手查询留存数据时改用 `RetentionTask`，只返回今天仍可处理的 `pending_review` 和 `send_failed` 任务；已经发送、冷却中、人工跟进、忽略或关闭的任务不会再次显示。
- 保留真实发送后的数据库状态：发送成功会写入客户通知，并把任务置为 `cooling`，下次查询会自动排除。

#### 为什么这样改？

之前查询使用旧的提醒日志，可能把已经发送过的客户再次当成待处理任务；前端也只更新了回复内容，没有保证消息列表停在最底部。现在查询直接读取发送流程使用的任务表，并按任务状态过滤，页面显示和数据库状态保持一致。

#### 如何验证？

- `python -m pytest -q tests/test_staff_graph.py`：16 passed。
- `python -m pytest -q tests/test_chat_agent.py tests/test_staff_agent_workbench_operations.py tests/test_retention_send_agent.py tests/test_retention_workbench.py`：28 passed。
- `python -m py_compile backend/agents/staff_graph.py backend/agents/tools.py backend/api/routers.py backend/agents/staff_operation_planner.py` 通过。
- `node --check frontend/staff.js` 和 `git diff --check` 通过。
- 浏览器打开 `http://127.0.0.1:8001/staff`，店长账号登录后确认侧栏和页面标题为“智能助手”；发送留存查询后，消息列表 `atLatest=true`；数据库中的冷却客户没有出现在查询回复中。

#### 面试怎么讲？
> 我把留存查询和留存发送统一到同一张 `RetentionTask` 任务表。发送成功后任务进入冷却状态，查询接口只读取仍可处理的状态，所以不会重复展示已发送客户。前端消息区使用固定最大高度和内部滚动，并在用户消息和 Agent 回复渲染后把滚动位置设置到最底部，保证最新对话始终可见。

#### 下一步

用户提出了“右键某条消息选择回复”的交互需求。下一步先确认消息引用的数据结构和后端上下文传递方案，再实现右键菜单、引用预览和带引用上下文的 Agent 请求。
### 第 78 步：智能助手消息右键回复和引用上下文（2026-07-16）
#### 这一步改了什么？
- 每条智能助手消息都有当前对话内的唯一编号，右键消息会弹出“回复”菜单。
- 选择“回复”后，输入框上方显示被引用消息的角色和内容预览，可以取消引用。
- 发送时请求增加结构化的 `reply_to` 字段，包含消息编号、角色和原文；当前用户的新问题仍单独保留。
- 后端把引用内容包装成上下文传给 Agent，并明确告诉 Agent：引用内容只是背景，当前新指令优先。
- 如果当前新输入只是“继续说明一下”这类追问，引用内容不会触发预约、退款、发送等工作台写操作；只有当前新输入明确表达操作意图时，才进入原有权限和业务流程。
- 补充“发一次消息”“发一条消息”“发一下”等自然语言表达，避免批量发送被误判成只读查询。

#### 为什么这样改？

右键回复的核心不是复制一段文字，而是让前后端都知道“用户正在回复哪一条历史消息”。前端负责选择和预览，后端负责校验和传递上下文，真正的 Agent 和工作台业务规则仍然走原来的统一入口。这样既能连续追问，也不会因为引用内容里的“预约”或“发送”等词误触发写操作。

#### 如何验证？

- `python -m pytest -q tests/test_staff_reply_context.py tests/test_staff_graph.py tests/test_chat_agent.py tests/test_staff_agent_workbench_operations.py tests/test_retention_send_agent.py tests/test_retention_workbench.py`：46 passed。
- `python -m py_compile backend/api/routers.py backend/api/schemas.py tests/test_staff_reply_context.py` 通过。
- `node --check frontend/staff.js` 和 `git diff --check` 通过。
- 浏览器打开 `http://127.0.0.1:8001/staff`，店长账号右键历史回复后看到“回复”菜单，选择后出现引用预览；发送请求包含 `reply_to`，Agent 能根据引用继续回答，且消息列表 `atLatest=true`。
- 额外验证“继续说明一下”不会误触发预约；“发一次消息”专项测试仍能进入批量发送流程。

#### 面试怎么讲？
> 我把消息引用设计成前后端共同遵守的数据结构，而不是只在浏览器里拼接字符串。前端负责消息编号、右键菜单和引用预览，后端负责把引用包装成上下文，并把当前新指令放在最后。对于写操作，我仍然只根据当前新输入判断是否执行，避免历史消息里的关键词造成误操作；真正执行时继续复用原有权限校验、事务和审计链路。

#### 下一步

右键引用回复已经完成。后续如果要增强，可以继续增加引用消息的快捷键操作、引用多条消息，以及在审计详情中展示本次 Agent 使用的引用上下文。

### 第 79 步：完成项目工程审查并生成项目报告（2026-07-16）

#### 这一步做了什么？

- 从应用入口、配置、中间件、认证、API、数据库、迁移、Agent、RAG、留存、财务、前端、小程序、部署和测试逐模块完成只读审查。
- 新增根目录 `项目报告.md`，记录总体架构、技术栈、模块职责、模块关联、问题分级、证据行号、测试结果、浏览器验证、面试话术和修复优先级。
- 用临时 SQLite 数据库验证了几个容易被正常测试遗漏的反例：客户自报消费、客户自升会员等级、已完成预约取消、停用账号登录。
- 用真实浏览器检查了客户 H5 和员工运营工作台，确认页面可加载、员工登录后主要接口返回 200，控制台没有错误。

#### 为什么这样做？

项目不能只看“测试是否通过”。正常测试证明预期流程能运行，反例审查才可以发现权限边界、状态机和数据来源问题。例如客户自报消费接口虽然返回结构正确，但它让客户直接制造财务和留存事实，这是业务权限问题，不是 Pydantic 格式问题。

#### 和哪些模块有关？

- 全部后端模块：FastAPI、JWT、业务服务、SQLAlchemy、Alembic、Agent、RAG、财务和留存。
- 全部客户端：客户 H5、员工工作台和微信小程序。
- 全部测试与部署材料：pytest、Docker、Docker Compose、运行文档和架构文档。

#### 如何验证？

- Python compileall、前端 JavaScript 语法检查、JSON 解析均通过。
- `python -m alembic upgrade head` 成功执行到 `0010_add_retention_workbench`。
- 预约/Agent 分组 32 passed，财务分组 15 passed，留存分组 26 passed，聊天/RAG 分组 31 passed；基础分组 20 passed、1 个迁移测试因 Windows 阻止 `alembic.exe` 失败。
- 全量测试收集到 115 个用例，但在 120 秒内未完成，报告中已记录测试执行效率风险。
- `/api/health` 返回 200；员工工作台登录后的预约、客户、会员、留存、退款、审计、钱包和概览接口均返回 200。

#### 面试怎么讲？

> 这次审查我把项目分成四层来验证：先看 API 和权限边界，再看业务服务和数据库事务，然后看 Agent 的提议、人工确认、执行和审计链路，最后用真实浏览器和测试验证前后端契约。最终结论不是简单说“项目没问题”，而是区分了演示级可用和生产级上线之间的差距。

#### 当前最重要的下一步

先修复 `项目报告.md` 中 F-001 到 F-007 的 P1 问题：禁止客户自写消费、限制会员等级和生日、补齐预约状态机、停用账号登录校验、日志脱敏、真实支付边界和真实消息渠道。修复每个问题时继续补反例测试，并再次更新本文件。

### 第 80 步：问题清单修复与修复后复审（2026-07-17）

#### 这一步改了什么？

- 客户消费写接口正式退役，`POST /api/transactions` 返回 410；消费、累计消费和积分只能从员工服务核验完成流程产生。
- 客户创建会员时生日同时经过格式校验和真实日期校验，会员等级不再信任客户端传入值，统一从 `silver` 起步。
- 预约取消增加 `pending/confirmed` 状态白名单；已完成或已取消预约不能再次取消。
- 登录和 JWT 认证都检查 `is_active`；浏览器 H5 和员工端改用 HttpOnly Cookie，并增加 logout 清理接口。
- 充值和 Mock 留存发送增加 `DEMO_MODE` 边界，生产模式直接关闭；小程序 API 地址移到 `frontend/miniprogram/config.js`。
- 时间槽增加数据库唯一约束；钱包、退款和服务核验增加行锁；预约消费记录增加唯一约束。
- 核心财务金额字段迁移为 `Numeric(12,2)`，钱包继续使用整数分；审计和 Agent 任务序列化支持 Decimal。
- 迁移测试改为 `sys.executable -m alembic`，README 测试数量更新为实际收集的 120 条。
- 新增安全回归测试，复审报告增加 F-001 至 F-019 的关闭状态和残余生产前置条件。

#### 为什么这样改？

审查发现最严重的问题不是页面能不能返回 200，而是谁有权制造业务事实。客户不能自己写消费、等级和积分；员工的高风险 Agent 操作也必须经过权限、提议和确认。数据库唯一约束、行锁和固定精度金额则负责防止并发请求把同一件业务重复记账。没有真实支付和微信渠道凭证时，系统明确关闭 Demo 入口，避免把演示能力误说成生产能力。

#### 和哪些模块有关？

- API 与认证：`backend/api/routers.py`、`backend/auth/security.py`、`backend/config.py`。
- 数据和事务：`backend/database/models.py`、`finance.py`、`service.py`、Alembic 0011-0013。
- 前端安全：`frontend/index.html`、`frontend/staff.js`、小程序 `app.js/config.js`。
- 测试和文档：安全回归、钱包、服务核验、迁移、README、`项目报告.md`。

#### 如何验证？

- 安全回归分组：18 passed。
- 财务、服务核验、迁移专项：修复 Decimal 序列化后通过。
- 迁移命令：`python -m alembic upgrade head` 执行到 0013。
- 测试收集：`pytest --collect-only -q` 收集 121 条。
- 还需要最后一次全量 `pytest -q` 和静态检查作为收尾证据，只有全量 0 failed 才能结束项目。

#### 面试怎么讲？

> 我先用反例验证权限边界，再按数据来源修复。客户不能提交消费结果，服务核验才是消费事实来源；会员等级由后端规则控制；预约取消使用状态白名单；财务字段用 Numeric 或整数分，并用唯一约束和行锁兜底。支付和微信发送没有真实第三方凭证时，我把入口封闭为 Demo，而不是把 Mock 说成生产接入。

#### 下一步

完成最后一次全量测试、静态检查和报告证据更新；确认没有失败后结束本轮项目。
